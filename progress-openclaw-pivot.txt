# Progress Log

## Learnings
(Patterns discovered during implementation)

- The retirement check tests have two phases: (1) "NOT SAFE TO RETIRE" tests that track active callers, and (2) after retirement, convert them to regression guards that assert the module stays deleted.
- When retiring a module, also check `tests/test_openclaw_contracts_audit.py` — its `CONTRACT_TO_LEGACY` dict maps contracts to legacy modules. Remove the entry when both the contract AND the legacy module are deleted.
- `rex.plugins.load_plugins()` returns `list[PluginSpec]` while the retired `rex.plugin_loader.load_plugins()` returned `dict[str, Any]`. To migrate: build `{f"plugins.{s.name}": s for s in specs}` for backward-compatible dict key access.
- All remaining `[ ]` tasks in the PRD are BLOCKED because OpenClaw is not installed. The only OPENCLAW-REPLACE module that could be retired without OpenClaw was `plugin_loader.py` (since its replacement `rex.plugins` already existed).

---

## Iteration 80 - Retire rex/plugin_loader.py
- Migrated root `voice_loop.py` from `rex.plugin_loader.load_plugins` to `rex.plugins.load_plugins`
- Adapted dict construction: `{f"plugins.{s.name}": s for s in _plugin_specs}` preserves `"plugins.web_search" in self.plugins` key-check semantics
- Deleted `rex/plugin_loader.py`
- Deleted `rex/contracts/plugins.py` (orphaned contract, required by contracts audit test)
- Updated `tests/test_retirement_check_plugin_loader.py` → retirement confirmation guard (2 tests: file deleted + no new importers)
- Updated `tests/test_openclaw_contracts_audit.py` → removed `plugins.py` from CONTRACT_TO_LEGACY, updated docstring
- Updated PRD: US-P7-003/004 now show DONE; Final Acceptance Criteria note updated from "all 8" to "7 of 8"
- Files changed: voice_loop.py, tests/test_retirement_check_plugin_loader.py, tests/test_openclaw_contracts_audit.py, PRD-openclaw-pivot-for-rex.md, progress-openclaw-pivot.txt
- Learnings for future iterations:
  - The blocked `[ ]` validation tasks (Dashboard, Messaging, Browser) all require OpenClaw to be installed before they can be completed
  - Next achievable retirement: executor.py (1 caller: rex/cli.py) — needs cli.py to stop using Executor class
  - Or: tool_router.py (2 callers: rex/assistant.py, rex/workflow_runner.py) — could use ToolBridge instead
---
