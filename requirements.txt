# Rex AI Assistant dependency entrypoints
#
# Base install (no ML/audio stack):
#   pip install .
#
# CPU ML stack (no CUDA):
#   pip install -r requirements-cpu.txt
#
# GPU CUDA 12.4 (RTX 3060 / Windows 11):
#   pip install -r requirements-gpu-cu124.txt
#
# Dev tooling:
#   pip install -e ".[dev]"
#
# This file is intentionally a pointer to the split requirements files.
