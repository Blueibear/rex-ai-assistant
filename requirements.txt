# ========================================
# Rex AI Assistant - Full Installation
# ========================================
#
# This installs ALL features. For minimal/selective install, use:
#   pip install -e .                    # Core only (no audio/TTS/STT/wakeword)
#   pip install -e .[full]              # All features (same as this file)
#   pip install -e .[audio,stt,tts]     # Specific features only
#
# Available extras: audio, stt, tts, wakeword, search, full, dev, test
# See pyproject.toml [project.optional-dependencies] for details
# ========================================

# Install package with full extras (includes all optional dependencies)
-e .[full]

# Additional development dependencies
pytest>=8.0.0

# Security-Critical Dependencies (already in pyproject.toml, listed here for documentation)
# cryptography>=43.0.1  # CVE fixes: GHSA-h4gh-qq45-vh27, GHSA-9v9h-cgj8-h64p
# setuptools>=78.1.1    # CVE fix: PYSEC-2025-49 (path traversal)
# pip>=25.3             # CVE fix: GHSA-4xh5-x5gv-qwph (tarfile path traversal)

# Note: PyTorch versions
# - Default: CPU-only (torch==2.6.0)
# - For GPU: pip install -e .[gpu-cu118] or use requirements-gpu.txt
# - torch 2.6.0 has local DoS vulnerabilities (GHSA-3749-ghw9-m3mg, GHSA-887c-mr87-cxwp)
#   Fixed in torch 2.8.0, upgrade when stable version is released

