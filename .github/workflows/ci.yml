name: CI

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: 🧰 Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg libsndfile1 libasound2-dev portaudio19-dev nvidia-cuda-toolkit

      - name: 📥 Install Python dependencies (with CUDA support)
        run: |
          python -m pip install --upgrade pip
          pip install torch==2.6.0+cu118 torchvision==0.21.0+cu118 torchaudio==2.6.0+cu118 --index-url https://download.pytorch.org/whl/cu118
          pip install -r requirements.txt
          pip install -r requirements-ml.txt || true  # Optional: for ML extras
          pip install -r requirements-dev.txt || true  # Optional: for dev tools
          pip install pytest pytest-cov

      - name: 🧪 Run tests with coverage
        run: |
          pytest --cov=. --cov-report=term-missing

      - name: 📤 Upload coverage report (optional)
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: .coverage
