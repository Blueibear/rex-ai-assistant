# Progress Log

## Cycle: Full Roadmap -- All 9 Phases
## PRD: outputs/PRD.md
## Stories: US-001 through US-078 (78 stories)

## Learnings
(Patterns discovered during implementation)

---

## Iteration 4 - US-004: Fix working directory resolution for bridge scripts
- repo_root() was already in bridge_utils.py (implemented in US-001); AC #1, 3, 4 were pre-satisfied
- Added `from rex.bridge_utils import repo_root, resolve_python` and `_REPO_ROOT = repo_root()` to all 16 root-level rex_*_bridge.py files
- No bridge script used os.getcwd() or relative paths; pattern is to expose the utility at module level
- ruff, mypy all pass clean; 8 existing tests pass
- Files changed: all 16 rex_*_bridge.py, PRD.md
- Learnings for future iterations:
  - The Edit tool requires a fresh Read call per file per conversation segment; batch reads at start
  - `_REPO_ROOT = repo_root()` follows the same module-level pattern as `_PYTHON_EXE = resolve_python()`
  - When AC say "use X()" and nothing currently needs it, add it as a module-level assignment anyway
---

## Iteration 3 - US-003: Replace raw python calls in GUI backend with venv resolver
- No subprocess calls exist in rex/gui_app.py — all AC were trivially satisfied
- mypy, ruff, and black all pass clean on the file
- Files changed: PRD.md only
- Learnings for future iterations:
  - gui_app.py uses webbrowser and Flask only — no subprocess spawning
  - Pattern: verify with grep before assuming work is needed; mark complete when already satisfied

---

## Iteration 2 - US-002: Replace raw python calls in bridge scripts with venv resolver
- Added `from rex.bridge_utils import resolve_python` and `_PYTHON_EXE = resolve_python()` to all 16 root-level rex_*_bridge.py files
- None of the bridge files had subprocess calls; criteria 2 and 3 were trivially met
- ruff auto-fixed 10 pre-existing UP017 violations (timezone.utc → datetime.UTC) during lint pass
- Files changed: all 16 rex_*_bridge.py, PRD.md
- Learnings for future iterations:
  - Bridge files are stdin/stdout JSON processors — they don't spawn subprocesses themselves
  - Module-level `_VAR = func()` is NOT flagged as unused by ruff (F841 only applies to locals)
  - ruff and black must be installed via pip (not in venv by default); run `pip install ruff black mypy`
  - Pre-existing unused-ignore mypy warnings exist in bridge files from `# type: ignore[import]` comments
---

## Iteration 1 - US-001: Add venv-aware Python resolver utility
- Created rex/bridge_utils.py with resolve_python() and repo_root()
- Created tests/test_bridge_utils.py with 8 tests (Windows, Linux, macOS paths, fallback, repo_root)
- Files changed: rex/bridge_utils.py, tests/test_bridge_utils.py, PRD.md
- Learnings for future iterations:
  - pytest and mypy are not pre-installed in the venv; install them before running
  - Use patch.object(bu.sys, "platform", ...) to simulate cross-platform path resolution
  - repo_root() walks up from __file__ looking for pyproject.toml — tests just validate the happy path
---

## Iteration 6 - US-006: Verify and create missing bridge scripts (shopping, speakers)
- Both scripts existed; rex_speaker_bridge.py silently fell back to {} on invalid JSON (AC violation)
- Fixed speaker bridge: invalid JSON now returns `{"ok": False, "error": "Bad input: ..."}` and returns
- Added `action` alias (`payload.get("action") or payload.get("command", ...)`) to both scripts
- Written tests/test_us006_bridge_json_io.py with 8 tests (list via command/action, invalid input, unknown command)
- ruff, black, mypy all pass; 8 new tests pass
- Files changed: rex_shopping_list_bridge.py, rex_speaker_bridge.py, tests/test_us006_bridge_json_io.py, PRD.md
- Learnings for future iterations:
  - Speaker bridge had a silent fallback on bad JSON — always test the invalid-input path explicitly
  - `_run_main()` pattern from US-005 reused cleanly; mocking `sys.modules` for lazy imports works well
---

## Iteration 5 - US-005: Verify and create missing bridge scripts (tasks, reminders, memory)
- All 3 scripts existed but used `"command"` key instead of `"action"` key specified in AC
- Added `payload.get("action") or payload.get("command")` in main() of all 3 scripts
- Written tests/test_us005_bridge_json_io.py with 12 tests (action/command aliases, invalid input, unknown action)
- ruff, black, mypy (pre-existing unused-ignore warnings only), 20 tests pass
- Files changed: rex_tasks_bridge.py, rex_reminders_bridge.py, rex_memories_bridge.py, tests/test_us005_bridge_json_io.py, PRD.md
- Learnings for future iterations:
  - Bridge scripts already had rich implementations; AC's "action" key just needed alias for "command"
  - Full pytest suite hits INTERNALERROR on Python 3.14 (project requires 3.11); run targeted tests instead
  - `_run_main()` pattern (mock sys.stdin/stdout, call mod.main()) cleanly tests bridge JSON I/O without subprocess
---

## Iteration 8 - US-008: Trace and fix voice pipeline capture-to-STT stage
- Added `sample_rate: int = 16000` param to `VoiceLoop.__init__` (stored as `self._sample_rate`)
- After `_record_phrase()`: logs `audio_capture_complete` with `audio_duration_s` and `audio_samples` fields
- Before `_transcribe()`: logs `stt_handoff` with `audio_samples` field
- Enhanced `except SpeechToTextError` to log `stt_error` event with "resetting pipeline" message
- Updated `build_voice_loop` to pass `sample_rate` to `VoiceLoop`
- Written tests/test_us008_voice_pipeline.py with 5 tests (capture log, duration field, handoff log, ordering, error reset)
- Tests skip without numpy (same pattern as US-007); 28 existing tests still pass
- ruff, black, mypy all pass (3 pre-existing mypy errors, none new)
- Files changed: rex/voice_loop.py, tests/test_us008_voice_pipeline.py, PRD.md
- Learnings for future iterations:
  - VoiceLoop.run() does not have direct access to sample_rate; add it as an __init__ param
  - `hasattr(audio, '__len__')` safely handles both numpy arrays and bytes for duration calc
  - numpy-dependent tests skip cleanly via pytest.importorskip at module level
---

## Iteration 7 - US-007: Trace and fix voice pipeline wake-to-capture stage
- Replaced `print("🎤 Wakeword DETECTED!")` in listener.py with `logger.info(..., extra={"event": "wakeword_detected", ...})`
- Added `logger.debug(..., extra={"event": "audio_capture_start", ...})` immediately before `yield frame`
- Added `import time` to listener.py to record monotonic timestamps in log extra fields
- Added empty/whitespace keyword validation in `build_default_detector` raising `WakeWordError` before model load
- Replaced all `print()` calls in `detect_wakeword` (utils.py) with `logger.debug()` calls
- Written tests/test_us007_wakeword_pipeline.py with 5 tests (structured log on detection, capture log event, order, empty/whitespace keyword raises WakeWordError)
- ruff, black pass; 28 existing tests pass; wakeword tests skip (numpy not installed — matches existing project pattern)
- Files changed: rex/wakeword/listener.py, rex/wakeword/utils.py, tests/test_us007_wakeword_pipeline.py, PRD.md
- Learnings for future iterations:
  - Wakeword tests use `pytest.importorskip("numpy")` — skip without numpy, same as test_wakeword.py
  - Pre-existing mypy error in rex/wakeword/embedding.py (no-redef for _torch) — not introduced by this work
  - Log `extra={"event": ...}` fields are accessible on LogRecord via `getattr(record, "event", None)` in caplog
---

## Iteration 9 - US-009: Trace and fix voice pipeline STT-to-LLM-to-TTS stage
- AC #1 (generate_reply) and AC #2 (speak called with response) were already satisfied
- Added explicit `except TextToSpeechError` handler in VoiceLoop.run() logging `tts_error` event with `llm_response` field
- Renamed local `response` to `llm_response` so it's accessible in the error handler
- Written tests/test_us009_voice_pipeline.py with 5 tests (generate_reply called with transcript, speak called with LLM response, TTS failure logs text, pipeline resets without hang, end-to-end happy path)
- Tests skip without numpy (same pattern as US-007/008); 28 existing tests still pass
- ruff, black, mypy all pass (3 pre-existing mypy errors, none new)
- Files changed: rex/voice_loop.py, tests/test_us009_voice_pipeline.py, PRD.md
- Learnings for future iterations:
  - `llm_response` must be initialized to None before the if/else streaming block so the error handler can reference it regardless of which branch ran
  - `getattr(assistant, "stream_reply", None)` combined with `del assistant.stream_reply` in tests forces the non-streaming path to exercise generate_reply
  - TextToSpeechError is imported from rex.assistant_errors (same module as SpeechToTextError)
---

## Iteration 11 - US-076: Remove mock calendar from voice loop runtime path
- Added `IntegrationNotConfiguredError` to `rex/assistant_errors.py`
- Updated `get_calendar_service()` to raise `IntegrationNotConfiguredError("Calendar: not configured")` for stub/unconfigured case instead of returning mock-backed service
- Updated `_create_ics_backed_service()` to raise instead of falling back to stub on empty source or failed ICS connect
- Changed `CalendarService.connect()` log from "mock mode" to "local storage"
- Updated `rex/assistant.py` to catch `IntegrationNotConfiguredError` with `info` logging (not `warning`)
- Moved `get_calendar_service()` inside try/except in `rex/integrations.py` and `rex/integrations/_setup.py`
- Updated `test_us045_calendar_integration.py` to test new behavior (raises when unconfigured)
- Written `tests/test_us076_calendar_mock_removal.py` with 8 tests
- 167 tests pass, 28 skipped; ruff and black pass
- Files changed: rex/assistant_errors.py, rex/calendar_service.py, rex/assistant.py, rex/integrations.py, rex/integrations/_setup.py, tests/test_us076_calendar_mock_removal.py, tests/test_us045_calendar_integration.py, PRD.md
- Learnings for future iterations:
  - `get_calendar_service()` now raises `IntegrationNotConfiguredError` when no ICS backend configured — callers must catch it
  - The global `_calendar_service` singleton is only set when ICS connects successfully
  - `CalendarService` direct instantiation still works fine for test fixtures using mock_events/mock_data_path
---

## Iteration 10 - US-075: Fix config type validation and coercion warnings
- Fixed config/rex_config.json: changed llm_temperature from string "0.7" to float 0.7 (config is gitignored so not committed)
- Updated _coerce_float() and _coerce_int() in rex/config.py to raise ConfigurationError (with field path and bad value) on unparseable inputs like "abc"
- Added check_config_types() function to rex/doctor.py capturing coercion warnings via logging.Handler, returning OK/WARNING/ERROR based on config state
- Called check_config_types(project_root) from run_diagnostics() after check_config_file()
- Written tests/test_us075_config_validation.py with 9 tests (invalid float raises, invalid int raises, error message includes bad value, valid coercion still works, doctor check for OK/WARNING/ERROR)
- ruff, black, 18 tests pass (9 new + 9 existing coercion tests)
- Files changed: rex/config.py, rex/doctor.py, tests/test_us075_config_validation.py, PRD.md
- Learnings for future iterations:
  - config/ directory is gitignored — changes to rex_config.json cannot be committed; note this but don't fail the task
  - Use a logging.Handler subclass to capture WARNING-level log output without interfering with test caplog
  - _coerce_float/int changes must preserve the happy path (non-string values still work via float(raw))
---

## Iteration 13 - US-078: Fix torio/FFmpeg runtime dependency for voice pipeline
- FFmpeg is NOT required for audio capture/playback (sounddevice); IS required for XTTS (torio dependency)
- Added torio warning suppression to rex/voice_loop.py (after all imports to satisfy ruff E402)
- Added check_ffmpeg_for_tts() to rex/doctor.py: WARNING if XTTS+no ffmpeg, INFO if edge-tts/pyttsx3+no ffmpeg, OK if found
- Removed ffmpeg from check_external_dependencies() (now has dedicated check_ffmpeg_for_tts())
- Integrated check_ffmpeg_for_tts() into run_diagnostics()
- 8 tests in tests/test_us078_ffmpeg_tts.py (all pass); ruff, black pass
- Files changed: rex/voice_loop.py, rex/doctor.py, tests/test_us078_ffmpeg_tts.py, PRD.md
- Learnings for future iterations:
  - warnings.filterwarnings() after imports causes ruff E402; place after all imports (TTS imports are lazy anyway)
  - Load_config imported locally inside function → patch via rex.config.load_config not rex.doctor.load_config
  - rex_loop.py and gui.py already had torio suppression; canonical package (rex/voice_loop.py) did not
---

## Iteration 12 - US-077: Pin XTTS/transformers compatible versions and patch BeamSearchScorer
- Added `transformers>=4.38.0,<5.0` pin to requirements-gpu-cu124.txt and requirements-gpu.txt
- `rex/compat/transformers_shims.py` and `_lazy_import_tts()` shim already existed (pre-satisfied)
- Modified `_speak_xtts()` in rex/voice_loop.py: instead of raising TextToSpeechError, logs warning and falls back to edge-tts when XTTS init fails
- Added `check_xtts_transformers_compat()` to rex/doctor.py and called it from `run_diagnostics()`
- Written tests/test_us077_xtts_compat.py with 7 tests (5 pass, 2 skip without numpy): doctor checks for INFO/WARNING/OK paths and edge-tts fallback with log warning
- ruff, black pass; 35 tests pass, 2 skipped
- Files changed: rex/voice_loop.py, rex/doctor.py, requirements-gpu-cu124.txt, requirements-gpu.txt, tests/test_us077_xtts_compat.py, PRD.md
- Learnings for future iterations:
  - async tests must use asyncio.run() in sync test functions (anyio/pytest-asyncio not installed)
  - MagicMock(spec=[]) gives hasattr() False for unlisted attrs; SimpleNamespace gives full control
  - `from rex.compat import ensure_transformers_compatibility` inside a function body picks up the patched version when rex.compat.ensure_transformers_compatibility is patched before the call
---

## Iteration 14 - US-010: Fix voice pipeline hang states
- Added `stt_timeout=30.0`, `llm_timeout=60.0`, `tts_timeout=30.0` params to `VoiceLoop.__init__`
- Wrapped `_transcribe`, `generate_reply`, and `_speak` awaits in `asyncio.wait_for()` in `run()`
- Streaming path wrapped with combined `llm_timeout + tts_timeout`
- On `TimeoutError`: logs `pipeline_timeout` event with `stage` field, then `continue` to re-enter listening state
- Tests skip without numpy (same pattern as US-007/008/009); 58 existing tests still pass
- ruff auto-fixed: `asyncio.TimeoutError` → builtin `TimeoutError` (UP041); black reformatted test file
- Files changed: rex/voice_loop.py, tests/test_us010_voice_pipeline_timeouts.py, PRD.md
- Learnings for future iterations:
  - ruff UP041: use builtin `TimeoutError` not `asyncio.TimeoutError` in Python 3.11+
  - `asyncio.wait_for()` accepts a coroutine directly — no need to wrap in `asyncio.ensure_future()`
  - `del assistant.stream_reply` on a MagicMock forces the non-streaming path (same pattern as US-009)
---

## Iteration 15 - US-011: Add missing voice dependencies to requirements
- Added `edge-tts>=6.1.9` and `pyttsx3>=2.90` to [project] dependencies in pyproject.toml
- Both are cross-platform (Windows/macOS/Linux) and lightweight (no GPU required)
- Written tests/test_us011_voice_deps.py with 5 tests (pyproject declaration, importability, correct section)
- 4 pass, 1 skipped (pyttsx3 not installed in test env); ruff, black pass
- Files changed: pyproject.toml, tests/test_us011_voice_deps.py, PRD.md
- Learnings for future iterations:
  - edge-tts and pyttsx3 were not in any dependency section; added to main [project] dependencies so `pip install .` picks them up
  - Both packages already had mypy ignore_missing_imports overrides in pyproject.toml (pre-existing)
  - Test regex `r'["\'"]?pkg["\'"]?\s*>='` matches both quoted and unquoted TOML dependency strings
---

## Iteration 16 - US-012: Fix custom voice duration validation
- Added `MAX_DURATION_SECONDS = 300.0` and `ACCEPTED_FORMATS` constants to `rex/custom_voices.py`
- Added explicit format check in `get_audio_duration()` raising ValueError with "Unsupported format: XYZ. Accepted formats: WAV, MP3, FLAC, OGG"
- Fixed too-short error message to "Sample is X.Xs, minimum is Ys" (was different wording)
- Added too-long check returning "Sample is X.Xs, maximum is Ys"
- Written tests/test_us012_custom_voice_duration.py with 9 tests using real WAV files created via stdlib wave module
- All 9 tests pass; ruff, black pass (black reformatted custom_voices.py inline strings)
- Files changed: rex/custom_voices.py, tests/test_us012_custom_voice_duration.py, PRD.md
- Learnings for future iterations:
  - Use stdlib `wave` + `struct.pack` to create real WAV files of known duration in tmp_path — no external deps
  - `struct.pack(f"<{n_frames}h", *([0] * n_frames))` writes silent PCM frames; efficient for test sizes
  - black reformats multi-line parenthesized f-strings to single lines; no behavior change
---

## Iteration 17 - US-013: Remove mock calendar data and connect real backend
- Removed `_default_mock_events()` from `rex/calendar_service.py` (returned hardcoded "Product sync" and "1:1 check-in" events)
- Updated `_load_events()` to return `[]` instead of calling `_default_mock_events()` when no file found
- Written `tests/test_us013_calendar_no_mock.py` with 10 tests (no fake events, not-configured raises, configured returns real events)
- 101 tests pass, 25 skipped; ruff, black pass
- Files changed: rex/calendar_service.py, tests/test_us013_calendar_no_mock.py, PRD.md
- Learnings for future iterations:
  - `_default_mock_events()` only used internally; safe to remove with no external callers
  - AC #2 "API returns not_configured" was already satisfied by US-076 (raises IntegrationNotConfiguredError)
  - Two separate CalendarService classes exist: `rex/calendar_service.py` (main) and `rex/integrations/calendar_service.py` (integrations layer); US-013 only targets the former
---

## Iteration 18 - US-014: Remove mock email data and connect real backend
- Removed hardcoded `_MOCK_EMAILS`, `_NOW`, `_dt` from `rex/email_backends/inbox_stub.py`
- `EmailInboxStub.__init__` now accepts `emails: list[EmailEnvelope] | None = None`; starts empty by default
- Added `_create_configured_email_service()` to `rex/email_service.py`; raises `IntegrationNotConfiguredError("Email: not configured")` when no IMAP credentials resolved
- Updated `get_email_service()` to call `_create_configured_email_service()` instead of returning `EmailService()` unconditionally
- Updated `rex/cli.py` `cmd_email` and `_cmd_email_send` to catch `IntegrationNotConfiguredError` and print friendly message
- Updated `tests/test_us078_email_inbox_stub.py`: added `_TEST_EMAILS` fixture list and `_stub_with_data()` helper; 7 data-dependent tests now inject test data
- Updated `tests/test_us079_email_triage.py` `TestMockInboxCoverage.setup_method` to inject test data via `_make_test_emails()`
- Written `tests/test_us014_email_not_configured.py` with 8 tests (stub empty, inject, not-configured raises, configured returns service)
- All 77 targeted tests pass; ruff and black pass
- Files changed: rex/email_backends/inbox_stub.py, rex/email_service.py, rex/cli.py, tests/test_us014_email_not_configured.py, tests/test_us078_email_inbox_stub.py, tests/test_us079_email_triage.py, PRD.md
- Learnings for future iterations:
  - `EmailInboxStub` was used in both test_us078 and test_us079; removing hardcoded data broke both test files
  - Pattern: move hardcoded test data into the test file itself, not into the stub class
  - `get_email_service()` follows same IntegrationNotConfiguredError pattern as `get_calendar_service()`
  - CLI callers need try/except around get_email_service(); check all call sites
---

## Iteration 19 - US-015: Replace "exit code 2" with meaningful error reporting
- Added `bridge_error_response(exc)` helper to `rex/bridge_utils.py` returning `{"ok": False, "error": str(exc), "traceback": traceback.format_exc()}`
- Updated 15 of 16 bridge scripts to use `bridge_error_response()` in their outer exception handlers; `rex_wakeword_list_bridge.py` skipped as it returns `ok: True` with warning (recovery path, not failure)
- For NDJSON bridges (`rex_chat_stream_bridge.py`, `rex_voice_bridge.py`), added `"traceback"` directly to the `{"type": "error", ...}` emit
- For `rex_stt_bridge.py` and `rex_speaker_bridge.py`, used `{**bridge_error_response(exc), "extra_key": ...}` to preserve extra fields
- Written 9 tests in `tests/test_us015_bridge_error_reporting.py`; all pass
- ruff, black pass; 9 tests pass
- Files changed: rex/bridge_utils.py, 15 rex_*_bridge.py files, tests/test_us015_bridge_error_reporting.py, PRD.md
- Learnings for future iterations:
  - `import traceback as _traceback` must come before `from rex.bridge_utils import ...` in bridge files (ruff I001 import order)
  - F841 "unused variable" fires for typed annotations like `mock_plugins: list = []` when var is never used — remove it
  - `{**bridge_error_response(exc), "extra_field": value}` cleanly merges the standard error dict with domain-specific extra fields
---

## Iteration 20 - US-016: Fix STT language handling for "auto" mode
- Normalised `"auto"` and `""` to `None` in `SpeechToText.__init__` (after reading from config)
- Added inner try/except in `_transcribe()`: if `self._language is None` and transcribe raises, logs warning and retries with `language="en"` (fallback)
- Written tests/test_us016_stt_language.py with 5 tests covering "auto", "", None, "en", and fallback path
- All 5 tests pass; ruff and black pass
- Files changed: rex/voice_loop.py, tests/test_us016_stt_language.py, PRD.md
- Learnings for future iterations:
  - Tests that call `stt.transcribe(audio=[], sample_rate=16000)` fail without numpy; patch `_prepare_audio_for_stt` and `_to_wav_buffer` to bypass
  - numpy check happens in `_prepare_audio_for_stt` for non-bytes inputs (list [] triggers it)
  - `_FAKE_WAV = b"RIFF\x00\x00\x00\x00WAVEfmt "` passes the WAV header check in transcribe()
---

## Iteration 22 - US-018: Fix wake word config mismatch and empty resolution
- Changed AppConfig.wakeword default from "rex" to "hey_rex"
- In load_config(): `_get_nested(..., "hey_rex") or "hey_rex"` normalises empty/None to "hey_rex"
- Added Path import + model-file existence check in build_default_detector() raising WakeWordError
- Added check_wakeword_config() to rex/doctor.py and wired into run_diagnostics()
- 8 tests pass, 2 skipped (numpy not installed — same pattern as US-007/008/009/010)
- ruff auto-fixed import ordering in test file; black passes
- Files changed: rex/config.py, rex/doctor.py, rex/wakeword/listener.py, tests/test_us018_wakeword_config.py, PRD.md
- Learnings for future iterations:
  - load_config() takes json_config= dict arg (not config_path=) for testing; use that to inject test JSON
  - check_wakeword_config() uses 'from .config import load_config' locally; patch rex.config.load_config with patch.object
  - `_get_nested(json_config, key, default) or fallback` correctly handles both missing key AND empty string
---

## Iteration 21 - US-017: Fix Whisper/STT runtime failure and error exposure
- Added `check_stt_backend()` to rex/doctor.py — checks `find_spec("whisper")` AND `find_spec("faster_whisper")`, returns OK/ERROR
- Called `check_stt_backend()` from `run_diagnostics()` before `check_stt_warmup()`
- Added `exc_info=True` to both STT error log calls in rex/voice_loop.py: in `_transcribe()` outer catch and in the main loop's `except SpeechToTextError` handler
- Written tests/test_us017_stt_backend.py with 6 tests (4 pass, 2 skip without numpy)
- ruff, black pass; 58 targeted tests pass, 5 skipped
- Files changed: rex/doctor.py, rex/voice_loop.py, tests/test_us017_stt_backend.py, PRD.md
- Learnings for future iterations:
  - `find_spec("faster_whisper")` uses underscore (Python module name), not hyphen
  - ruff B017 fires on `pytest.raises(Exception)` — add `# noqa: B017` to suppress
  - The main loop's SpeechToTextError already `continue`s (no hang) — AC #4 was pre-satisfied
---

## Iteration 23 - US-019: Fix XTTS/transformers compatibility issues
- AC #1-#4 were pre-satisfied: `_lazy_import_tts()` already uses `find_spec("TTS")`, `ensure_transformers_compatibility()` called before import, `rex/tts_utils.py` has no heavy imports
- Fixed `speak()` in rex/voice_loop.py: when `_provider == "xtts"` and `_xtts_init_error` is set, log error with "XTTS not initialized ({reason})" instead of the downstream exception message
- Fixed 3 numpy-dependent tests to use `pytest.importorskip("numpy")` instead of bare `import numpy as np` (consistent with US-007/008/009 skip pattern)
- Added missing `import pytest` to test_us019_text_to_speech.py
- 11 pass, 3 skipped (numpy not installed); US-077 tests still pass (5 pass, 2 skip)
- ruff, black pass
- Files changed: rex/voice_loop.py, tests/test_us019_text_to_speech.py, PRD.md
- Learnings for future iterations:
  - `mock_logger.error.call_args[0][1]` is the first format arg, not the full rendered string — the message must be in that arg
  - US-077 edge-tts fallback in `_speak_xtts` is compatible with US-019 error logging because: when fallback succeeds (US-077), no error is logged; when fallback fails (no soundfile), `speak()` now logs the XTTS init error instead of the soundfile error
  - Always add `import pytest` when using `pytest.importorskip` in test files that don't already import it
---

## Iteration 24 - US-020: Fix FFmpeg/torio errors and config coercion warnings
- AC #1: Added `import shutil` + `shutil.which("ffmpeg")` check after TTS init in `build_voice_loop()`; logs WARNING with `event="ffmpeg_missing"` when provider is "xtts" and FFmpeg absent
- AC #2: Already satisfied by US-078 (`check_ffmpeg_for_tts()` in doctor.py)
- AC #3: Already satisfied by US-075 (`_coerce_float/_coerce_int` raise `ConfigurationError` on bad values)
- AC #4/#5: Verified `load_config()` raises no `UserWarning` or `DeprecationWarning`; tested via `warnings.catch_warnings(record=True)`
- Written `tests/test_us020_ffmpeg_config_warnings.py` with 8 tests (all pass)
- ruff auto-fixed one violation in the test file; black reformatted it
- Files changed: rex/voice_loop.py, tests/test_us020_ffmpeg_config_warnings.py, PRD.md
- Learnings for future iterations:
  - `patch.multiple("rex.voice_loop", key=mock)` keys must be bare attribute names on the module, not full dotted paths
  - `build_default_detector` is locally imported inside `build_voice_loop()` — patch target is `rex.wakeword.listener.build_default_detector` but requires the module to be in sys.modules first
  - When numpy is absent, stub `rex.wakeword` and `rex.wakeword.listener` in sys.modules using `types.ModuleType` before calling into `build_voice_loop()` — restore original entries in a finally block
  - Warning scope for FFmpeg: only XTTS requires FFmpeg (torio dependency); edge-tts and pyttsx3 do not → warning is scoped to `tts._provider == "xtts"`
---

## Iteration 25 - US-021: Add Music Assistant HTTP client
- Created `rex/integrations/music_assistant.py` with `MusicAssistantClient` class
- Methods: `play(query, room=None)`, `pause(room=None)`, `resume(room=None)`, `skip(room=None)`, `set_volume(level, room=None)`
- Uses stdlib `urllib.request` (no extra deps); lazy imports inside `_post()` 
- Added `music_assistant_url` and `music_assistant_token` fields to `AppConfig` in `rex/config.py`
- All methods raise `IntegrationNotConfiguredError` when `base_url` is empty/None
- 16 tests in `tests/test_us021_music_assistant.py` — all pass; ruff, black pass
- Files changed: rex/integrations/music_assistant.py, rex/config.py, tests/test_us021_music_assistant.py, PRD.md
- Learnings for future iterations:
  - ruff UP045 fires on `Optional[X]` annotations — use `X | None` syntax in new files
  - Inline imports inside methods need ruff I001 sort order (json, urllib.error, urllib.request alphabetically)
  - `patch.object(Client, "_post", return_value=rv)` cleanly mocks HTTP without real network calls
---

## Iteration 26 - US-022: Wire Music Assistant commands to assistant tool routing
- Created `rex/music_handler.py` with `MusicHandler` class using regex intent detection (play, pause, resume, skip, volume)
- Added room targeting: "play jazz in the kitchen" extracts room from `in the <room>` phrase
- Added `IntegrationNotConfiguredError` fallback: returns "Music Assistant is not set up." when client unconfigured
- Added 5 music tools to `rex/tools/registry.py` (music_play, music_pause, music_resume, music_skip, music_volume), each requiring `music_assistant_url`
- Wired `MusicHandler` into `Assistant.__init__()` (after shopping list handler) and `generate_reply()` (intercepts before LLM)
- 35 tests in `tests/test_us022_music_handler.py` — all pass; ruff auto-fixed 4 violations, black passes
- Files changed: rex/music_handler.py (new), rex/tools/registry.py, rex/assistant.py, tests/test_us022_music_handler.py (new), PRD.md
- Learnings for future iterations:
  - `patch("rex.assistant.MusicAssistantClient")` fails because the import is lazy (inside `__init__`); use `Assistant.__new__()` + direct attribute injection instead
  - Sentinel pattern for "matched but no room" vs "no match": use tuple `(matched: bool, room | None)` — cleaner than using a sentinel object with `str | None` type hint
  - `_match_room_command(patterns, transcript)` helper avoids duplicating the match loop across pause/resume/skip intents
---

## Iteration 27 - US-023: Add room context system
- Created `rex/context/__init__.py` and `rex/context/room.py` with `RoomContext` class
- Priority: explicit > speaker_origin > last_active > config_default
- `current_room` property iterates sources in priority order, returns first non-None
- Setters: `set_explicit`, `clear_explicit`, `set_speaker_origin`, `set_last_active`, `set_config_default`
- 8 tests in `tests/test_us023_room_context.py` — all pass; ruff, black, mypy all pass
- Files changed: rex/context/__init__.py (new), rex/context/room.py (new), tests/test_us023_room_context.py (new), PRD.md
- Learnings for future iterations:
  - `rex/context/` is a new package — needs `__init__.py` before mypy/ruff will type-check cleanly
  - Simple priority resolution: iterate a tuple of sources in priority order, return first non-None
---

## Iteration 28 - US-024: Add speaker origin detection to room context
- Added `set_speaker_origin_from_topic(topic)` to RoomContext: parses `rex/audio/<room>` MQTT topics via regex
- Added `set_speaker_origin_from_device(device_id, device_room_map)` to RoomContext: dict lookup
- Added `device_room_map: Dict[str, str]` field to AppConfig (default empty dict)
- 11 tests in tests/test_us024_speaker_origin.py: MQTT topic match/no-match, device mapping, fallback, priority
- ruff, black, pytest all pass
- Files changed: rex/context/room.py, rex/config.py, tests/test_us024_speaker_origin.py, PRD.md
- Learnings for future iterations:
  - ruff I001 fires if `from __future__ import annotations` is not in its own block separated from stdlib imports
  - `re.compile(r"^rex/audio/([^/]+)$")` neatly matches the required MQTT topic pattern
  - device_room_map uses `field(default_factory=dict)` — same pattern as ha_entity_map but with non-Optional type
---

## Iteration 29 - US-025: Add device alias system with synonym and fuzzy matching
- Created `rex/ha/__init__.py` and `rex/ha/device_aliases.py` with `AliasResolver` class
- Pure stdlib Levenshtein implementation (no external deps); max distance 2
- `resolve(query)` returns `(entity_id, confidence)` or `None`; confidence 1.0 exact, 0.95 synonym-exact, 0.9/0.8 fuzzy
- Created `config/device_aliases.json` with empty aliases and sample synonyms (lamp->light, telly->tv)
- 10 tests in `tests/test_us025_device_aliases.py` (exact, case-insensitive, fuzzy 1-edit, fuzzy 2-edit, beyond threshold, synonym, no match, empty file, missing file)
- ruff, black, mypy all pass
- Files changed: rex/ha/__init__.py (new), rex/ha/device_aliases.py (new), config/device_aliases.json (new), tests/test_us025_device_aliases.py (new), PRD.md
- Learnings for future iterations:
  - `rex/ha/` is a new package — needs `__init__.py` for mypy/ruff to type-check cleanly
  - Pure stdlib Levenshtein avoids adding `python-Levenshtein` or `rapidfuzz` as dependencies
  - Synonym expansion: replace individual words before looking up the full phrase; avoids O(n²) substring matching
---

## Iteration 30 - US-026: Add device discovery via Home Assistant API
- Created `rex/ha/discovery.py` with `discover_devices()` function
- Calls HA `/api/states` via stdlib `urllib.request` (no extra deps)
- Returns list of `{entity_id, friendly_name, domain, state}` dicts
- Module-level cache with configurable TTL (default 300 s / 5 min); `clear_cache()` and `set_cache_ttl()` for control
- Returns `[]` and logs WARNING when `base_url` or `token` is not set
- Returns `[]` and logs ERROR on HTTP errors
- 8 tests in `tests/test_us026_ha_discovery.py` — all pass; ruff, black, mypy all pass
- Files changed: rex/ha/discovery.py (new), tests/test_us026_ha_discovery.py (new), PRD.md
- Learnings for future iterations:
  - `autouse=True` fixture to reset module-level cache state is cleaner than per-test `clear_cache()` calls
  - `_mock_urlopen()` helper returns a context-manager MagicMock; `__enter__` returns itself, `read()` returns encoded JSON
  - `ssl_ctx=None` in `urlopen(..., context=None)` uses the default SSL verification — no need to pass it when verify_ssl=True
---

## Iteration 31 - US-026/US-027: Device discovery PRD fix + Device approval workflow
- US-026 PRD ACs were unchecked despite implementation existing; marked [x] after confirming 8 tests pass
- Added `approve_device(entity_id, alias, aliases_path)` and `ignore_device(entity_id, ignore_path)` to `rex/ha/discovery.py`
- Added `load_ignored_devices(ignore_path)` helper to read ignore list
- Extended discovery.py with json/Path imports and `_DEFAULT_ALIASES_PATH`/`_DEFAULT_IGNORE_PATH` constants
- Added `_cmd_ha_approve()` to `rex/cli.py`: loads HA config, discovers devices, filters approved/ignored, prompts user
- Registered `rex ha approve` subparser with `--aliases-path` and `--ignore-path` override flags
- 14 tests in `tests/test_us027_device_approval.py`: 9 pass, 5 skipped (CLI tests skip on Python 3.14)
- ruff auto-fixed 2 violations (import order, f-string); black reformatted 2 files
- Files changed: rex/ha/discovery.py, rex/cli.py, tests/test_us027_device_approval.py, PRD.md
- Learnings for future iterations:
  - `rex.cli` raises SystemExit on Python != 3.11; use `sys.version_info[:2] == (3, 11)` + `@pytest.mark.skipif` for CLI tests
  - `@pytest.mark.skipif` on a helper function (not a test) is harmless but also not needed; only mark actual test functions
  - ruff I001: imports inside functions must be sorted alphabetically (rex.config before rex.ha.device_aliases)
  - `resolver._aliases.values()` accesses internal state — acceptable for test/CLI code since it avoids a public API round-trip
---

## Iteration 32 - US-028: Add device state awareness
- Created `rex/ha/device_state.py` with `get_device_state()`: calls HA `/api/states/<entity_id>`, returns `{entity_id, state, attributes}` or None on 404/not configured/error
- Created `rex/ha/state_handler.py` with `DeviceStateHandler`: regex-detects "is X on?", "what's the status of X?", "check state of X?" questions, resolves alias via AliasResolver, queries HA, returns spoken answer
- Wired `DeviceStateHandler` into `Assistant.__init__()` and `generate_reply()` (after music handler, before per-user scoping)
- 11 tests in `tests/test_us028_device_state.py` — all pass; ruff, black, mypy all pass
- Files changed: rex/ha/device_state.py (new), rex/ha/state_handler.py (new), rex/assistant.py, tests/test_us028_device_state.py (new), PRD.md
- Learnings for future iterations:
  - urllib.error.HTTPError must be caught inside `_fetch_entity_state`; returning None for 404 and re-raising otherwise
  - Pattern for mocking urlopen: make a context-manager MagicMock where `__enter__` returns inner mock with `.read()` → encoded JSON
  - ruff I001: local imports inside test methods must be sorted (stdlib before project imports)
  - DeviceStateHandler follows exact same pattern as MusicHandler (lazy import, getattr guard in generate_reply)
---
  - DeviceStateHandler follows exact same pattern as MusicHandler (lazy import, getattr guard in generate_reply)
---

## Iteration 33 - US-029: Add command confirmation and undo support
- Created rex/ha/command_history.py with CommandHistory (FIFO deque, max_size=5, undo_window=30s) and CommandEntry dataclass
- HABridge.process_transcript() now pushes successful commands to _command_history after execution
- Added HABridge.undo_last(window=30.0) that pops last reversible command and executes inverse service
- Inverse map: turn_on↔turn_off, lock↔unlock, open_cover↔close_cover (set_temperature etc. are not reversible)
- Added _UNDO_PATTERN to rex/assistant.py; generate_reply() checks undo intent before process_transcript
- 16 tests in tests/test_us029_command_confirmation_undo.py (all pass); ruff, black pass
- Files changed: rex/ha/command_history.py (new), rex/ha_bridge.py, rex/assistant.py, tests/test_us029_command_confirmation_undo.py (new), PRD.md
- Learnings for future iterations:
  - Module-level constants (like _UNDO_PATTERN) must come AFTER all imports or ruff E402 fires
  - F841 fires for unused local variables even in short blocks — remove or use them
  - ruff I001 auto-fixable with --fix; always run fix pass before check pass on test files
  - async tests must use asyncio.run() in sync test functions — @pytest.mark.asyncio requires pytest-asyncio
---

## Iteration 34 - US-030: Add clarification system for ambiguous commands
- Added `AliasResolver.resolve_all(query, min_confidence=0.7)` returning all `(alias, entity_id, confidence)` candidates sorted desc
- Created `rex/ha/clarification.py` with `ClarificationHandler` class detecting:
  - Pronoun-based missing device ("turn it on" → "Which device would you like to control?")
  - Ambiguous entity: two candidates within confidence delta 0.15 → "Did you mean X or Y?"
- Wired `ClarificationHandler` into `HABridge.process_transcript()` before intent matching
- `HABridge.__init__` creates `AliasResolver()` using optional `ha_aliases_path` config or default path
- 15 tests in `tests/test_us030_clarification.py` — all pass; ruff auto-fixed 3 violations, black reformatted 2 files
- Files changed: rex/ha/device_aliases.py, rex/ha/clarification.py (new), rex/ha_bridge.py, tests/test_us030_clarification.py (new), PRD.md
- Learnings for future iterations:
  - "Similar confidence" ambiguity: use a delta threshold (0.15) — exact match (1.0) vs fuzzy (0.9) has delta 0.1 → triggers; exact (1.0) vs far fuzzy (0.8) has delta 0.2 → doesn't trigger
  - Test cases for fuzzy ambiguity: use two aliases like "bedroom light" and "bedroom lights" — one is exact match, other is 1-edit from the exact query → within delta
  - AC #3 ("pipeline re-enters listening") is satisfied automatically by the existing voice loop architecture — returning a string causes TTS + loop back
  - `HABridge.__new__()` in tests needs only `_base_url`, `_token`, and `_clarification` set; other attrs aren't needed when clarification triggers before `_match_transcript`
---

## Iteration 35 - US-031: Add error recovery with alternative suggestions
- Created `rex/ha/error_recovery.py` with `suggest_alternatives()` and helpers
- `_extract_room()` extracts room name from entity_id suffix (e.g. `light.kitchen_ceiling` → `"kitchen"`)
- `_same_room_alternatives()` finds same-domain, same-room candidates from entity_map + entity_cache
- `suggest_alternatives()` merges same-room + recently-used alternatives; produces spoken TTS message
- Added `recent_entity_ids()` method to `CommandHistory` (returns entity IDs oldest-first)
- Updated `HABridge.process_transcript()`: on command failure, calls `suggest_alternatives` instead of generic error string
- 15 tests in `tests/test_us031_error_recovery.py` — all pass; ruff, black pass
- Files changed: rex/ha/error_recovery.py (new), rex/ha/command_history.py, rex/ha_bridge.py, tests/test_us031_error_recovery.py (new), PRD.md
- Learnings for future iterations:
  - ruff F821 fires on bare type annotation referencing a locally-imported class; use string annotation + `# noqa: F821`
  - Room extraction: first word of the entity suffix after the dot (`light.kitchen_ceiling` → `kitchen`)
  - `{**entity_cache, **entity_map}` — entity_map (config) takes precedence over entity_cache (discovery)
---

## Iteration 36 - US-032: Add tool auto-selection system
- AC #1 (tool_catalog.py registry): Added `CatalogEntry` dataclass and `TOOL_CATALOG` list with 6 entries (weather_now, web_search, ha, calendar, send_email, time_now); each has `intent_patterns`, `capability_tags`, and `requires_config`
- AC #2 (generate_reply uses pattern matching): Already satisfied by existing `ToolDispatcher` wired into `Assistant.generate_reply()`
- AC #3 (highest confidence chosen): Replaced flat tag iteration in `select_tools()` with confidence scoring; score = count of tool's capability_tags in `fired_tags`; sorted descending so highest-confidence first
- AC #4 (LLM fallback): Already satisfied; `fired_tags` empty → return `[]` → `_tool_context` is None → LLM path
- AC #5 (3 routing tests): weather, HA, calendar routing tested; also no-match empty list and confidence ordering
- AC #6 (typecheck): mypy passes clean on both changed files
- 12 new tests pass; 15 existing dispatcher tests still pass; ruff auto-fixed 3 violations in test file
- Files changed: rex/tool_catalog.py, rex/tools/dispatcher.py, tests/test_us032_tool_auto_selection.py, PRD.md
- Learnings for future iterations:
  - `_INTENT_RULES` maps capability_tag → compound regex (OR of keywords); "forecast" is IN the weather regex but NOT its own rule key — don't create test tools with tags that aren't rule keys
  - Intent rule keys: "email", "weather", "search", "calendar", "smart_home" — only these 5 can appear in `fired_tags`
  - Existing `test_compound_question_selects_multiple_tools` must stay green; confidence scoring must NOT reduce multi-domain queries to single tool
  - Stable sort (`list.sort()`) preserves registration order for tied scores — matches expected determinism in tests
---

## Iteration 37 - US-033: Add perceived speed system (post-STT acknowledgment)
- Added `acknowledgment_mode: str = "sound"` to `AppConfig` in `rex/config.py` (values: "sound", "phrase", "none")
- Added `_get_nested(json_config, "acknowledgment.mode", "sound")` to `load_config()`
- Added `post_stt_acknowledge` callable parameter to `VoiceLoop.__init__` and stored as `self._post_stt_acknowledge`
- Added `_safe_post_stt_acknowledge()` method to `VoiceLoop` (suppresses errors, same pattern as `_safe_acknowledge`)
- In `VoiceLoop.run()`: after transcript is validated non-empty, calls `_safe_post_stt_acknowledge()` before LLM call
- In `build_voice_loop()`: reads `acknowledgment_mode`; "phrase" → speak "On it", "sound" → play chime, "none" → None
- Written 6 tests in `tests/test_us033_acknowledgment.py`: 2 config tests (always pass), 4 VoiceLoop tests (skip without numpy)
- ruff, black pass; 2 tests pass, 4 skipped
- Files changed: rex/config.py, rex/voice_loop.py, tests/test_us033_acknowledgment.py, PRD.md
- Learnings for future iterations:
  - `pytest.importorskip("numpy")` at MODULE LEVEL skips the entire file (including tests before it); use `@pytest.mark.skipif(find_spec("numpy") is None, ...)` decorators on individual tests if some tests don't need numpy
  - The existing pre-STT ack (fires at wake word detection, concurrent with recording) is separate from the new post-STT ack (fires after transcription, before LLM)
  - `noqa: E731` needed on lambda assignment in `build_voice_loop()`; ruff auto-fixes remove it otherwise
---
