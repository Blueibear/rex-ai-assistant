# Progress Log

## Learnings
(Patterns discovered during implementation)

---

## Iteration 1 - US-001: Create OpenClaw HTTP client module
- Created `rex/openclaw/errors.py` with OpenClawConnectionError, OpenClawAuthError, OpenClawAPIError all inheriting from AssistantError
- Created `rex/openclaw/http_client.py` with OpenClawClient (post/get/patch/delete, retry logic, auth headers) and get_openclaw_client() singleton
- Created `tests/test_openclaw_http_client.py` with 19 tests using unittest.mock
- Files changed: rex/openclaw/errors.py (new), rex/openclaw/http_client.py (new), tests/test_openclaw_http_client.py (new)
- Learnings for future iterations:
  - The dev venv is at `venv/` not `.venv/` — use `venv/Scripts/pytest.exe` and `venv/Scripts/python.exe`
  - `pydantic` was missing from the venv but installed on demand; may need other deps too
  - mypy complained about `return response.json()` (Any return) — assign to typed variable first: `result: dict[str, Any] = response.json(); return result`
  - ruff UP037 flagged quoted forward refs in `_CLIENT_CACHE: dict[str, "OpenClawClient"]` — fixed automatically with `--fix`
  - ruff I001 flagged import ordering in test file — fixed automatically
  - `requests` is already in pyproject.toml dependencies (>=2.32.0), no new dep needed
  - The `responses` library is NOT in dev deps; use `unittest.mock` for HTTP mocking
---

## Iteration 2 - US-002: Add OpenClaw gateway config fields to AppConfig
- Added `openclaw_gateway_url`, `openclaw_gateway_timeout`, `openclaw_gateway_max_retries` to `AppConfig` dataclass
- Added `openclaw_gateway_token: Optional[str]` loaded from `OPENCLAW_GATEWAY_TOKEN` env var
- Added all four fields to `build_app_config()` using `_get_nested()` and `os.getenv()`
- Updated `config/rex_config.json` openclaw section (local only — not committed; `config/` is gitignored)
- Added `OPENCLAW_GATEWAY_TOKEN=` with doc comment to `.env.example`
- Files changed: `rex/config.py`, `.env.example`, `PRD-openclaw-http-integration.md`
- Learnings for future iterations:
  - `config/rex_config.json` is in `.gitignore` (line 67: `config/`) with exemptions only for `.example.json` and `.schema.json` — changes to rex_config.json are local-only, never committed
  - Pre-existing test failures exist in `test_openclaw_ha_tts_voice_loop.py` etc. — they fail due to missing `pytest-asyncio`, not our changes
  - `get_openclaw_client()` singleton in `http_client.py` accepts `AppConfig` — after US-002, it can use the new `openclaw_gateway_url`, `openclaw_gateway_timeout`, `openclaw_gateway_max_retries`, and `openclaw_gateway_token` fields directly
---

## Iteration 3 - US-003: Wire OpenAI strategy to OpenClaw chat completions
- Added `user: str | None = None` kwarg to `OpenAIStrategy.generate()` — passed as `api_params["user"]` when not None
- Added `_user_key` derivation in `LanguageModel.generate()`: uses `user_id` if non-default, else `active_profile`; only passed when not None (guard: `if _user_key is not None`)
- Created `tests/test_openclaw_chat_completions.py` with 9 tests covering: user field present/absent, model field, authorization via api_key, messages format, non-openai provider gets no user field
- Added HTTP Integration section (§8) to `docs/openclaw-agent-setup.md` with arch diagram, config walkthrough, session persistence explanation, quick start, and troubleshooting table
- Files changed: `rex/llm_client.py`, `tests/test_openclaw_chat_completions.py`, `docs/openclaw-agent-setup.md`
- Learnings for future iterations:
  - `AppConfig` has a `__post_init__` validator: if `llm_provider == "openai"`, `openai_model` MUST be set — always set both `llm_model` and `openai_model` in OpenAI test configs
  - Many pre-existing test collection errors exist for autonomy/us1xx test files (import errors unrelated to our work) — ignore them when checking test suite health; test by running only relevant files
  - Passing `user=None` as a kwarg to strategies that don't accept it raises TypeError (caught by existing fallback), so always guard with `if _user_key is not None`
  - `EchoStrategy.generate()` does accept `messages=` but NOT `user=` — the `**extra` pattern is the right approach for optional kwargs
---
