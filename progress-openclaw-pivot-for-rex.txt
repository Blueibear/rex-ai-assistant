## Iteration 53 - US-P4-018: Update remaining event consumers to use EventBridge
- Updated 3 remaining subscribers to use EventBridge():
  1. `rex/integrations.py:setup_default_event_handlers()` — log_email_event, log_calendar_event
  2. `rex/integrations/_setup.py:setup_default_event_handlers()` — same
  3. `rex/event_triggers.py:EventTriggerRegistry.__init__()` — wildcard subscriber default
- Changed `EventTriggerRegistry` type hint from `EventBus | None` to `EventBus | EventBridge | None`; default from `get_event_bus()` to `EventBridge()`
- Updated 3 test files to patch `rex.openclaw.event_bridge.EventBridge` instead of `get_event_bus` where subscriber behavior is asserted
- Publisher closures (setup_email_job, setup_calendar_job) still use get_event_bus() for event_bus.publish() calls — unchanged
- 58 tests pass across 4 test files
- Files changed: rex/integrations.py, rex/integrations/_setup.py, rex/event_triggers.py, tests/test_integrations_setup.py, tests/test_rex_integrations.py, PRD-openclaw-pivot-for-rex.md, progress-openclaw-pivot-for-rex.txt
- Learnings for future iterations:
  - Publishers (closures capturing event_bus) can remain on raw get_event_bus() for now
  - When patching EventBridge, target `rex.openclaw.event_bridge.EventBridge` not module-local alias
---

## Iteration 52 - US-P4-017: Update first event consumer to use EventBridge
- Picked simplest subscriber: `rex/notification.py:setup_event_subscriptions()`
- Changed from `from rex.event_bus import get_event_bus; bus = get_event_bus()` to `from rex.openclaw.event_bridge import EventBridge; bus = EventBridge()`
- EventBridge() wraps the same global bus singleton → functionally identical, now routes through bridge
- Updated existing `test_notifier_event_subscription` in tests/test_notification.py to patch `rex.openclaw.event_bridge.EventBridge` instead of `rex.event_bus.get_event_bus`
- 18 notification tests pass
- Files changed: rex/notification.py, tests/test_notification.py, PRD-openclaw-pivot-for-rex.md, progress-openclaw-pivot-for-rex.txt
- Learnings for future iterations:
  - US-P4-018 remaining consumers: rex/integrations.py and rex/integrations/_setup.py (both log_email_event + log_calendar_event subscribers), rex/event_triggers.py (wildcard attach())
  - Each consumer just needs `get_event_bus()` → `EventBridge()` swap
  - Any existing tests that patch `rex.event_bus.get_event_bus` for that consumer must be updated to patch `rex.openclaw.event_bridge.EventBridge`
---

## Iteration 51 - US-P4-015 + US-P4-016: Create event bridge and test round-trip
- Created `rex/openclaw/event_bridge.py` (EventBridge class, ~175 lines)
  - Implements EventBusProtocol structurally
  - Delegates all 8 protocol methods to underlying EventBus
  - Accepts explicit bus arg (defaults to global singleton via get_event_bus())
  - register() stub follows ToolBridge pattern — logs warning if openclaw not installed
- Created `tests/test_openclaw_event_bridge.py` with 27 tests across 5 classes:
  - TestEventBridgeInstantiation (5): import, no-args, explicit bus, bool flag, protocol conformance
  - TestDelegation (10): every method delegates with correct args
  - TestRoundTrip (10): legacy callback, rich handler, wildcard, unsubscribe, legacy unsub callable, clear_subscriptions, rich Event object, metrics, subscription count, bus/bridge shared state
  - TestRegister (2): returns None without openclaw, accepts agent arg
- 27 tests pass in 0.31s
- Files changed: rex/openclaw/event_bridge.py, tests/test_openclaw_event_bridge.py, PRD-openclaw-pivot-for-rex.md, progress-openclaw-pivot-for-rex.txt
- Learnings for future iterations:
  - EventBridge must use get_event_bus() default so all existing Rex subscribers receive events
  - bridge._bus is the same object as the bus passed in — shared state test verifies this correctly
  - US-P4-017 should pick the simplest subscriber: notification.py (email.unread handler)
---

## Iteration 50 - US-P4-014: Classify each event type
- Classified all 10 event types found in US-P4-013 audit
- Result: 0 framework-level, 10 Rex-specific, 10 bridge-needed (all Rex events need bridging during migration)
- EventBus mechanism itself is framework-level (Replace); event *types* are Rex-specific
- Documented classification table and wildcard subscriber note in docs/openclaw-migration-status.md
- Updated event_bus.py row status to "Audited (US-P4-014)"
- Key insight: wildcard subscriber (EventTriggerRegistry) requires bridge to fan-out all events — if OpenClaw lacks wildcard sub, bridge must implement fan-out itself
- No new code files (pure classification/documentation task)
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md, progress-openclaw-pivot-for-rex.txt
---

## Iteration 49 - US-P4-013: List all event types on event bus
- Audited `rex/event_bus.py` (436 lines): dual API (legacy string + rich Event), EventBus + EventQueue classes, global singleton via `get_event_bus()`
- Grepped all publish calls across codebase; found 13 event types:
  - email.*: email.unread, email.triaged, email.read
  - calendar.*: calendar.connected, calendar.upcoming, calendar.range, calendar.created, calendar.updated, calendar.deleted, calendar.update
- Found 7 subscribers: notification.py (email.unread, calendar.upcoming), event_triggers.py (wildcard "*"), integrations.py and integrations/_setup.py (both publish same calendar events — parallel implementations)
- Updated `docs/openclaw-migration-status.md`: row status → "Audited (US-P4-013)", appended full event type and subscriber tables, classification findings for US-P4-014
- No new test files (pure audit/documentation task)
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md, progress-openclaw-pivot-for-rex.txt
- Learnings for future iterations:
  - `rex/integrations.py` and `rex/integrations/_setup.py` are parallel implementations publishing same calendar event types — US-P4-014 classification should note this duplication
  - EventBus mechanism is framework-level (Replace), all payload event types are Rex domain events (Keep/Bridge)
  - EventTriggerRegistry in event_triggers.py uses wildcard `"*"` subscribe — bridge must preserve this wildcard capability
---

## Iteration 48 - US-P4-012: Test assistant with both tool paths
- Created `tests/test_openclaw_tool_path_parity.py` with 9 tests across 3 classes:
  - TestRouterParity (5): non-tool text unchanged, empty string, tool request routed, unknown tool, skip_policy_check — both paths produce identical results
  - TestBridgeDelegation (2): bridge calls same underlying fn, bridge and legacy share same module object
  - TestAssistantPathParity (2): generate_reply non-tool parity, generate_reply tool parity
- 9 tests pass in ~7 min (async tests require tokenizer init)
- Files changed: tests/test_openclaw_tool_path_parity.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - ToolBridge.route_if_tool_request delegates to the same underlying _route_if_tool_request — parity is structural/guaranteed
  - `_bridge_route` helper needs to forward `skip_policy_check` kwarg explicitly
  - Patch `rex.tool_router.execute_tool` (not the bridge module) when testing route_if_tool_request round-trips
  - US-P4-013 should grep for rex.event_bus publish/subscribe calls and document in migration status
---

## Iteration 47 - US-P4-011: Update assistant.py to use tool bridge with feature flag
- Renamed import alias: `route_if_tool_request as _legacy_route_if_tool_request` in assistant.py
- Added `_tool_router_fn` selection in `Assistant.__init__`: imports ToolBridge and binds its method when `use_openclaw_tools=True`, else uses legacy fn
- Updated `generate_reply` to call `self._tool_router_fn` instead of hardcoded `route_if_tool_request`
- Created `tests/test_openclaw_assistant_flag.py` with 6 tests across 2 classes:
  - TestToolRouterFnSelection (3): legacy fn when False, bridge fn when True, two instances independent
  - TestGenerateReplyRouting (3): generate_reply calls legacy, bridge, correct args passed
- 6 new tests pass; 6 assistant tests pass; no regressions
- Files changed: rex/assistant.py, tests/test_openclaw_assistant_flag.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - `Settings = AppConfig` alias in rex/config.py — assistant.py uses Settings but gets AppConfig fields including `use_openclaw_tools`
  - `object.__setattr__` works to override frozen-ish dataclass fields in tests (AppConfig is not truly frozen)
  - Tests using asyncio.mode=STRICT need `@pytest.mark.asyncio` decorator; generate_reply tests are async
  - These async tests run very slowly (~2 min each) due to LLM/tokenizer init side effects in fixture setup
  - US-P4-012 needs to confirm both paths produce equivalent results; can use mock LLM to avoid slow tokenizer
---

## Iteration 46 - US-P4-010: Add feature flag for tool routing
- Added `use_openclaw_tools: bool = False` to `AppConfig` dataclass in `rex/config.py`
- Added parsing in `build_app_config`: reads `openclaw.use_tools` (default False)
- Updated `config/rex_config.json` with `"openclaw": {"use_tools": false}` section (gitignored)
- Created `tests/test_openclaw_feature_flag.py` with 7 tests across 2 classes
  - TestUseOpenclawToolsDefault (4): has field, default False, build_app_config default False, absent section
  - TestUseOpenclawToolsEnabled (3): reads True, reads False, coerces to bool
- 7 new tests pass; 27 config tests pass; no regressions
- Files changed: rex/config.py, tests/test_openclaw_feature_flag.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - `config/rex_config.json` is in .gitignore — update it locally but don't stage it
  - Use `openclaw.use_tools` as JSON key (maps cleanly to `use_openclaw_tools` in AppConfig)
  - US-P4-011 should check `cfg.use_openclaw_tools` in `assistant.py` to route to ToolBridge vs old tool_router
---

## Iteration 45 - US-P4-009: Test HA tools through OpenClaw
- Created `tests/test_openclaw_ha_tools_e2e.py` with 10 tests across 4 classes
  - TestHaToolPolicyGuard (2): guard raises ApprovalRequiredError; LOW-risk override passes
  - TestHaToolPolicyCheck (1): check returns requires_approval=True, allowed=True, denied=False
  - TestRexAgentCallToolHaGate (2): call_tool raises ApprovalRequiredError; execute_tool not called when blocked
  - TestHaCallServiceExecution (5): turn_on succeeds, turn_off succeeds, bridge failure propagated, not-configured returns error, intent fields match args
- 23 HA-related tests pass; no regressions
- Files changed: tests/test_openclaw_ha_tools_e2e.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - home_assistant_call_service is explicitly in DEFAULT_POLICIES (MEDIUM, allow_auto=False) — so no special override needed; guard() always raises
  - Service-level tests follow same mock pattern: patch _get_ha_bridge, set enabled=True, set _execute_intent return value
  - US-P4-010 should add USE_OPENCLAW_TOOLS config flag to AppConfig/rex_config.json
---

## Iteration 44 - US-P4-008: Register HA tools
- Created `rex/openclaw/tools/ha_tool.py` with `ha_call_service(domain, service, entity_id, data=None, context=None)`
  - TOOL_NAME = "home_assistant_call_service" (matching DEFAULT_POLICIES in policy_engine.py)
  - `_get_ha_bridge()` factory (patchable at module level) returns `HABridge()`
  - Checks `bridge.enabled` first; returns error dict if HA not configured (avoids RuntimeError)
  - Builds `IntentMatch` dataclass and delegates to `bridge._execute_intent(intent)`
  - `register()` stub follows same OPENCLAW_AVAILABLE guard + warning pattern
- Added `register_ha_tools(agent=None) → dict` to `ToolBridge`
  - Imports `_register_ha_call_service` at module level (patchable)
  - Returns dict with "home_assistant_call_service" key
- Created `tests/test_openclaw_ha_tools.py` with 13 tests across 2 classes
  - TestHaTool (7): import, constants, calls _execute_intent, data merged, not-enabled returns error, context ignored, register stub
  - TestRegisterHaTools (6): returns dict, has key, no extra keys, calls register fn, forwards agent, none without openclaw
- 271 total openclaw tests pass; no regressions
- Files changed: rex/openclaw/tools/ha_tool.py, rex/openclaw/tool_bridge.py, tests/test_openclaw_ha_tools.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - HABridge has no factory function; use `_get_ha_bridge() -> HABridge()` pattern (patchable via module-level import)
  - HABridge.__init__ calls `_require_requests()` which raises if requests not installed — always mock `_get_ha_bridge` in tests
  - `bridge.enabled` check must come BEFORE `_execute_intent` call (returns False if no base_url+token configured)
  - IntentMatch is a @dataclass with fields: domain, service, entity_id, data, description, source
  - US-P4-009 should test HA commands execute correctly (mock bridge.enabled=True + _execute_intent return value)
---

## Iteration 43 - US-P4-007: Test policy-gated tools
- Created `tests/test_openclaw_policy_gated_tools_e2e.py` with 16 tests across 3 classes
  - TestPolicyGateGuard (6): guard() raises ApprovalRequiredError for send_email/send_sms/calendar_create; low-risk override allows each
  - TestPolicyGateCheck (5): check() returns requires_approval=True for all three tools; denied=True for blocked recipient/domain
  - TestRexAgentCallToolPolicyGate (5): call_tool raises for all three tools; raises PolicyDeniedError for blocked recipient; service not called when blocked
- 418 targeted tests pass; no regressions
- Files changed: tests/test_openclaw_policy_gated_tools_e2e.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - send_sms and calendar_create are NOT in DEFAULT_POLICIES; they fall back to the default MEDIUM-risk policy (unknown tools require approval)
  - DEFAULT_POLICIES uses "calendar_create_event" (not "calendar_create") — the tool bridge uses the shorter name
  - PolicyEngine(policies=[...]) overrides DEFAULT_POLICIES only for matching tool names; others keep defaults
  - Patch "rex.tool_router.execute_tool" (not the bridge) when testing call_tool() service-not-called scenario
  - US-P4-008 should register ha_bridge functions via the ToolBridge (similar pattern to register_policy_gated_tools)
---

## Iteration 42 - US-P4-006: Register policy-gated tools batch
- Created `rex/openclaw/tools/email_tool.py` (send_email → EmailService.send)
- Created `rex/openclaw/tools/sms_tool.py` (send_sms → SMSService.send via Message)
- Created `rex/openclaw/tools/calendar_tool.py` (calendar_create → CalendarService.create_event with ISO string parsing)
- Added `register_policy_gated_tools(agent=None) → dict` to ToolBridge
  - Returns dict with send_email, send_sms, calendar_create keys
  - Imports _register_send_email, _register_send_sms, _register_calendar_create at module level
- Created `tests/test_openclaw_policy_gated_tools.py` with 26 tests across 5 classes
  - TestEmailTool (5): import, constants, send delegates to service, to-list, context ignored, register stub
  - TestSmsTool (5): import, constants, calls service, Message to/body correct, context ignored, register stub
  - TestCalendarTool (6): import, constants, delegates to service, datetime objects, naive string→UTC, optional fields, register stub
  - TestRegisterPolicyGatedTools (6): returns dict, has keys, no extra keys, calls each fn, forwards agent, none values without openclaw
- 242 total openclaw tests pass; no regressions
- Files changed: rex/openclaw/tools/email_tool.py, rex/openclaw/tools/sms_tool.py, rex/openclaw/tools/calendar_tool.py, rex/openclaw/tool_bridge.py, tests/test_openclaw_policy_gated_tools.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Message model requires `channel` and `from_` (alias "from") — must pass `channel="sms"` and `**{"from": ""}` since from_ is required but SMSService overrides empty from_ with its configured from_number
  - Policy-gated tool callables do NOT call tool_router (tool_router doesn't implement send_email/calendar_create); they call the actual services directly
  - TOOL_NAME = "send_email" / "send_sms" / "calendar_create" — use short names matching the style of existing tools
  - _parse_dt() helper handles both ISO strings and datetime objects; adds UTC tzinfo to naive datetimes
  - US-P4-007 should test ApprovalRequiredError/PolicyDeniedError raised when calling these tools through PolicyAdapter without skip_policy_check
---

## Iteration 41 - US-P4-005: Test simple tools through OpenClaw
- Created `tests/test_openclaw_simple_tools_e2e.py` with 18 tests across 4 classes
  - TestRegisterThenExerciseTimeTool (3): register_simple_tools returns handle dict; time_now and weather_now callable after register
  - TestTimeNowThroughBridge (7): returns dict, contains local_time/date/timezone, location arg forwarded, real call returns dict, real call returns time keys or error
  - TestWeatherNowThroughBridge (5): returns dict, success structure, location arg forwarded, real call with skip_credential_check=True returns dict or error
  - TestSimpleToolsViaRouteIfToolRequest (3): time_now routed + model re-called, weather_now routed + model re-called, non-tool text unchanged
- 216 total openclaw tests pass; no regressions
- Files changed: tests/test_openclaw_simple_tools_e2e.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - `route_if_tool_request` delegates to `rex.tool_router.route_if_tool_request` which calls `rex.tool_router.execute_tool` internally — patch `rex.tool_router.execute_tool` (not `rex.openclaw.tool_bridge._execute_tool`) when testing route_if_tool_request end-to-end
  - weather_now raises `CredentialMissingError` when `skip_credential_check=False` and no API key — real weather tests need `skip_credential_check=True`
  - US-P4-006 should register policy-gated tools (send_email, calendar_create) via bridge
---

## Iteration 40 - US-P4-004: Register simple read-only tools batch
- Added `register_simple_tools(agent=None) -> dict` to `ToolBridge`
- Imports `_register_time_now` and `_register_weather_now` at module level (patchable)
- Returns dict mapping tool name → registration handle; geolocation not in tool_router so documented as gap
- Created `tests/test_openclaw_tool_bridge_register.py` with 11 tests
  - Returns dict, has time_now/weather_now keys, no extra keys
  - Calls each register fn, forwards agent arg, stores returned handles
  - no_agent uses None default, returns None values without openclaw
- 198 total openclaw tests pass; no regressions
- Files changed: rex/openclaw/tool_bridge.py, tests/test_openclaw_tool_bridge_register.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Import individual register fns at module level to enable patching: `from rex.openclaw.tools.time_tool import register as _register_time_now`
  - Patch target: `rex.openclaw.tool_bridge._register_time_now` (not tool_tool.register)
  - geolocation is not in tool_router supported_tools; embedded in time_now/weather_now fallback — no standalone tool needed
  - US-P4-005 should test the simple tools through OpenClaw agent (call register_simple_tools then exercise tools)
---

## Iteration 39 - US-P4-003: Create tool bridge
- Created `rex/openclaw/tool_bridge.py` with `ToolBridge` class implementing `ToolRoutingProtocol`
- Delegates: parse_tool_request → _parse_tool_request, execute_tool → _execute_tool, route_if_tool_request → _route_if_tool_request
- All kwargs (skip_policy_check, skip_credential_check, task_id, requested_by, skip_audit_log) forwarded through
- Same OPENCLAW_AVAILABLE guard + register() stub pattern as other adapters
- Created `tests/test_openclaw_tool_bridge.py` with 20 tests across 5 classes
  - TestToolBridgeInstantiation (4): import, no-args, OPENCLAW_AVAILABLE type, isinstance(bridge, ToolRoutingProtocol)
  - TestParseToolRequest (5): valid, non-tool, empty, with-args, delegation check
  - TestExecuteTool (6): delegation, skip_policy, skip_credential, task_id, requested_by, skip_audit_log
  - TestRouteIfToolRequest (3): non-tool passthrough, delegation, skip_policy_check
  - TestRegister (2): returns None without openclaw, accepts agent arg
- 187 total openclaw tests pass; no regressions
- Files changed: rex/openclaw/tool_bridge.py, tests/test_openclaw_tool_bridge.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - ToolBridge implements ToolRoutingProtocol structurally — isinstance check passes with @runtime_checkable
  - Import _parse_tool_request etc at module level (not lazily) to enable patching in tests
  - Patch target: `rex.openclaw.tool_bridge._execute_tool` (not `rex.tool_router.execute_tool`)
  - US-P4-004 should register time_now, weather_now (already have tool files) via bridge
---

## Iteration 38 - US-P4-002: Classify each tool by migration type
- Classified all 11 DEFAULT_POLICIES tools as: Generic (replace), Adapter-needed, or Rex-specific
- Generic (replace) × 6: time_now, weather_now, web_search, execute_command, file_write, file_delete
- Adapter-needed × 3: send_email, calendar_create_event, calendar_delete_event
- Rex-specific × 2: home_assistant_call_service, pc_run
- Appended full classification table + migration sequencing notes to docs/openclaw-migration-status.md
- Updated tool_router.py row status to "Audited (US-P4-002)"
- 151 openclaw tests pass; no regressions
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Classification: only 3 tools have implementations in tool_router; other 8 are policy-gated stubs
  - US-P4-003 (tool bridge) should start with the 3 implemented generics (time_now, weather_now, web_search)
  - Adapter-needed tools need bridges to rex.email_service and rex.calendar_service
  - Rex-specific tools (home_assistant_call_service, pc_run) move to Phase 5 as skills
---

## Iteration 37 - US-P4-001: Audit tool_router.py tool name/handler mapping
- Read rex/tool_router.py and rex/tool_registry.py in full
- Found supported_tools = {"time_now", "weather_now", "web_search"} (only 3 handlers in execute_tool)
- Handler dispatch: time_now → _execute_time_now; weather_now → _execute_weather_now; web_search → _execute_web_search
- Tool registry has 5 builtins: time_now, weather_now, web_search, send_email (stub), home_assistant
- Gap: 8 of 11 DEFAULT_POLICIES tools have no handler in tool_router.py (policy-gated but return "Unknown tool")
- pc_run handled via pc_run_policy.py + approval flow, not through tool_router
- Appended full audit table to docs/openclaw-migration-status.md; updated tool_router row status
- 67 targeted openclaw tests pass; no regressions
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Only 3 tools are fully implemented in tool_router: time_now, weather_now, web_search
  - skip_policy_check=True forces skip_credential_check=True — the two flags are coupled
  - tool_registry has send_email and home_assistant registered as stubs (health returns False)
  - US-P4-002 (tool routing bridge) only needs to cover the 3 implemented tools; stubs documented
---

## Iteration 36 - US-P3-019: Test approval adapter end-to-end
- Created `tests/test_openclaw_approval_adapter.py` with 31 tests across 7 classes
- TestApprovalAdapterInstantiation (4): import, no-args, approval_dir property, OPENCLAW_AVAILABLE flag
- TestApprovalAdapterCreate (7): returns WorkflowApproval, status pending, workflow_id/step_id stored, persists to disk, optional fields, unique IDs
- TestApprovalAdapterLoad (3): load after create, None for unknown id, preserves workflow_id
- TestApprovalAdapterListPending (4): empty dir, contains new approval, multiple, only shows pending
- TestApprovalAdapterApprove (4): returns True, updates status to approved, False for unknown, removes from pending
- TestApprovalAdapterDeny (4): returns True, updates status to denied, False for unknown, removes from pending
- TestApprovalAdapterEndToEnd (3): create→list→approve→reload, create→list→deny→reload, multiple independent
- 167 total openclaw tests pass; no regressions
- Files changed: tests/test_openclaw_approval_adapter.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - approve_workflow/deny_workflow return True if record found+updated, False if not found
  - list_pending_approvals only returns records with status=="pending"
  - tmp_path isolation works cleanly — each test has its own empty approval dir
---

## Iteration 35 - US-P3-018: Create approval adapter
- Created `rex/openclaw/approval_adapter.py` with `ApprovalAdapter` class
- Methods: create(workflow_id, step_id, ...) → persists new WorkflowApproval; load(approval_id); approve(approval_id); deny(approval_id); list_pending()
- All delegation: create/load → WorkflowApproval.save/load; approve/deny/list_pending → workflow_runner helpers
- Same OPENCLAW_AVAILABLE guard + register() stub pattern as PolicyAdapter and IdentityAdapter
- approval_dir injectable on constructor (defaults to DEFAULT_APPROVAL_DIR) for test isolation
- 97 existing openclaw tests pass; module imports cleanly
- Files changed: rex/openclaw/approval_adapter.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - approve_workflow/deny_workflow/list_pending_approvals all accept approval_dir kwarg — adapter passes its injected path through
  - WorkflowApproval.load() returns None if file not found — adapter exposes this directly
  - US-P3-019 tests: create → list_pending sees it → approve/deny → reload shows updated status
---

## Iteration 34 - US-P3-017: Audit approval system
- Read `rex/workflow.py` (WorkflowApproval model, DEFAULT_APPROVAL_DIR, generate_approval_id, save/load)
- Read `rex/workflow_runner.py` (_make_approval, resume_after_approval, approve_workflow, deny_workflow, list_pending_approvals, ApprovalBlockedError)
- Read `rex/executor.py` (passes approval_dir through; surfaces blocking_approval_id in result)
- Read `rex/computers/pc_run_policy.py` (creates approvals for pc_run using WorkflowApproval with sentinel workflow_id)
- Read `rex/woocommerce/write_policy.py` (creates approvals for WC write actions using same pattern)
- Read `rex/cli.py` cmd_approvals (user-facing list/approve/deny/show)
- Read `rex/tool_router.py` (raises ApprovalRequiredError in-memory only — does NOT write files)
- Appended full audit notes to `docs/openclaw-migration-status.md`
- 76 openclaw tests pass; no regressions
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - `WorkflowApproval` in `rex/workflow.py` is the single approval record type; all producers and consumers use it
  - Three approval producers: `workflow_runner._make_approval()`, `pc_run_policy.check_pc_run_policy()`, `woocommerce/write_policy.check_wc_write_policy()`
  - `tool_router.py` raises `ApprovalRequiredError` in-memory; the CALLER (workflow_runner) writes the file
  - Non-workflow callers (pc_run, woocommerce) use deterministic step_id (SHA-256) so re-runs find the same record
  - US-P3-018 `ApprovalAdapter` should wrap: `WorkflowApproval.save/load`, `approve_workflow`, `deny_workflow`, `list_pending_approvals`
---

## Iteration 33 - US-P3-016: Wire profile manager into OpenClaw agent
- Added `apply_profile_to_config(config, profile_name, profiles_dir)` to `rex/openclaw/config.py`
  - Uses `dataclasses.replace()` to create a modified AppConfig with updated capabilities and active_profile
  - Imports `load_profile` from profile_manager; overwrites `capabilities` wholesale (matching apply_profile semantics)
- Added `profile_name: str | None = None` kwarg to `RexAgent.__init__`
  - When set, calls `apply_profile_to_config` before `build_agent_config`/`build_system_prompt`
  - Also imported `load_config` at module level (not lazily) to enable patching in tests
- Created `tests/test_openclaw_profile.py` with 15 tests across 4 classes
  - TestApplyProfileToConfig (6): capabilities replaced, active_profile set, new instance, other fields preserved, empty caps, missing profile
  - TestBuildSystemPromptWithProfile (3): caps in prompt, profile name in prompt, default omitted
  - TestBuildAgentConfigWithProfile (2): caps in rex_capabilities, active_profile in dict
  - TestRexAgentProfileName (4): None leaves unchanged, updates prompt caps, updates active_profile, missing raises
- 113 total openclaw tests pass; no regressions
- Files changed: rex/openclaw/config.py, rex/openclaw/agent.py, tests/test_openclaw_profile.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Use `dataclasses.replace(config, field=value)` to get a modified AppConfig copy — clean and type-safe
  - `apply_profile` replaces capabilities wholesale; overrides use nested JSON structure (not flat AppConfig keys) — only capabilities and active_profile reliably map to AppConfig for persona
  - Import module-level (not lazy) to enable patching: `from rex.config import load_config` at top of agent.py
  - Patch `rex.openclaw.config.apply_profile_to_config` (not `rex.openclaw.agent.*`) since it's used via lazy import in __init__
---

## Iteration 32 - US-P3-015: Audit profile_manager.py public API
- Read rex/profile_manager.py (97 lines): 3 public functions + 1 constant in __all__
- Public API: DEFAULT_PROFILES_DIR, load_profile(name, profiles_dir), apply_profile(base_config, profile), get_active_profile_name(config)
- Merge behavior: apply_profile() deep-merges profile["overrides"] dict into base_config; capabilities list is wholesale-replaced (not merged)
- Callers: rex/config.py (lines 235-238, all 3 functions at config load time); tests/test_profile_manager.py only
- Key finding: profile is baked into AppConfig BEFORE build_agent_config() runs — no direct OpenClaw wiring needed in profile_manager itself
- US-P3-016 is verifying build_agent_config()/build_system_prompt() reflect profile-applied AppConfig, not adding new profile loading
- Updated docs/openclaw-migration-status.md: changed status to "Audited (US-P3-015)", appended full audit notes
- 86 tests pass
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - apply_profile() replaces capabilities list wholesale — not deep-merged — important distinction
  - Profile affects AppConfig at construction time; by the time openclaw/config.py runs, profile is already applied
  - US-P3-016 test: create AppConfig with non-default capabilities, verify build_agent_config() includes them
---

## Iteration 31 - US-P3-014: Test identity adapter
- Created `tests/test_openclaw_identity_adapter.py` with 21 tests across 6 classes
- TestIdentityAdapterInstantiation (4): import, no-args, config dict, OPENCLAW_AVAILABLE flag
- TestSetAndGetUser (4): set→get propagation, None session, delegation to set/get_session_user
- TestClearUser (2): delegates to clear_session_user, get returns None after clear
- TestResolveUser (4): explicit priority, session fallback, None return, config passed through
- TestBuildSession (5): required keys, set→build propagation, delegation, metadata passthrough, no-user
- TestRegister (2): returns None when openclaw not installed (with and without agent arg)
- Patch pattern: `rex.openclaw.identity_adapter.{fn}` for all mocked identity/session functions
- Files changed: tests/test_openclaw_identity_adapter.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - build_session() passes `config=None` when _config is empty dict (falsy check: `if self._config else None`)
  - resolve_user() passes `config={}` (empty dict, not None) — always passes _config directly
  - mock_ctx called with positional explicit_user + keyword config/metadata in build_session delegation
---

## Iteration 30 - US-P3-013: Create identity adapter
- Created `rex/openclaw/identity_adapter.py` with `IdentityAdapter` class
- Methods: get_user() / set_user(id) / clear_user() → delegates to get/set/clear_session_user
- resolve_user(explicit_user=None) → delegates to resolve_active_user with config fallback
- build_session(explicit_user, metadata) → delegates to build_session_context from session.py
- register(agent=None) stub with OPENCLAW_AVAILABLE guard (same pattern as PolicyAdapter)
- Profile CRUD (create_user_profile, get_user_profile, etc.) NOT wrapped — kept as Rex file ops
- 61 targeted tests pass (session, agent_basic, memory, policy); module imports cleanly
- Files changed: rex/openclaw/identity_adapter.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - IdentityAdapter.build_session() returns the same dict as build_session_context()
  - US-P3-014 tests: set_user() then get_user() + build_session() to verify propagation
  - Use tmp-based session state override if needed (identity.py reads LOCALAPPDATA/rex-ai/session.json)
---

## Iteration 29 - US-P3-012: Audit identity.py public API
- Read rex/identity.py (322 lines): 9 public functions in __all__
- Session state (3 fns): get/set/clear_session_user — OS temp file (LOCALAPPDATA\rex-ai\session.json on Windows)
- Resolution (2 fns): resolve_active_user (4-level priority), require_active_user (SystemExit on failure)
- Profile CRUD (4 fns): create/get/update_user_profile, list_known_users — Memory/ directory layout
- Callers: cli.py (identify cmd), openclaw/session.py (already wrapped), voice_identity/fallback_flow.py
- Updated docs/openclaw-migration-status.md with full audit notes
- 36 tests pass
- Learnings for future iterations:
  - US-P3-013 identity adapter wraps session+resolution fns; profile CRUD kept as Rex file ops
  - openclaw/session.py already uses resolve_active_user — identity adapter bridges the gap
---

## Iteration 28 - US-P3-011: Wire policy adapter into OpenClaw agent
- Added `policy_adapter` param to `RexAgent.__init__`; defaults to `PolicyAdapter()`
- Added `call_tool(tool_name, args, metadata)` method: guard() first, then execute_tool() with skip_policy_check=True
- PolicyDeniedError/ApprovalRequiredError propagate from guard() to caller
- 54 tests pass; no regressions
- Files changed: rex/openclaw/agent.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - call_tool() is the hook to test in a future "agent calls tool, policy blocks it" integration test
  - skip_policy_check=True in execute_tool() because policy is already enforced by the adapter
---

## Iteration 27 - US-P3-010: Test policy adapter allow path
- Added TestPolicyAdapterAllowPath (10 tests) to tests/test_openclaw_policy.py
- Tests: guard() passes for time_now/weather_now/web_search (DEFAULT_POLICIES low-risk), custom low-risk tool, check() fields correct, register() returns None, engine property, OPENCLAW_AVAILABLE type
- 18 total tests in file; all pass
- Files changed: tests/test_openclaw_policy.py, PRD-openclaw-pivot-for-rex.md
---

## Iteration 26 - US-P3-009: Test policy adapter block path
- Created `tests/test_openclaw_policy.py` with 8 block-path tests (TestPolicyAdapterBlockPath)
- Tests: guard raises ApprovalRequiredError (medium, high, custom, unknown); guard raises PolicyDeniedError (blocked recipient, blocked domain); check() returns correct decision fields
- Helper _adapter(*extra_policies) injects fresh PolicyEngine — no global singleton state
- 8 tests pass
- Files changed: tests/test_openclaw_policy.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - US-P3-010 allow-path tests go in same file (TestPolicyAdapterAllowPath class)
  - PolicyEngine(policies=[...]) overrides DEFAULT_POLICIES for same tool_name
---

## Iteration 25 - US-P3-008: Create policy adapter
- Created `rex/openclaw/policy_adapter.py` with `PolicyAdapter` class
- Methods: check(tool_name, metadata) -> PolicyDecision; guard() raises PolicyDeniedError/ApprovalRequiredError; register() stub
- Imports PolicyDeniedError/ApprovalRequiredError from rex.tool_router for consistency
- Injectable engine param (PolicyEngine or get_policy_engine() singleton)
- 55 tests pass; module imports cleanly
- Files changed: rex/openclaw/policy_adapter.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - guard() is the method to test for US-P3-009 block path
  - check() returns PolicyDecision directly for US-P3-009 allow path
---

## Iteration 24 - US-P3-007: Audit policy.py and policy_engine.py public API
- Read both modules: policy.py (159 lines), policy_engine.py (406 lines)
- PolicyDecision consumers: tool_router.py (primary gateway), pc_run_policy.py, workflow_runner.py, executor.py, browser_automation.py, cli.py (6 callers)
- 11 policy-gated tools in DEFAULT_POLICIES: 3 LOW auto-execute, 4 MEDIUM approval, 4 HIGH approval
- Central enforcement point: execute_tool() in tool_router.py (skip_policy_check=True bypasses)
- Both modules marked # OPENCLAW-WRAP; PolicyEngine is injectable (no forced globals)
- Updated docs/openclaw-migration-status.md: rows changed to "Audited (US-P3-007)", full audit notes appended
- 43 tests pass
- Learnings for future iterations:
  - PolicyEngine adapter (US-P3-008) wraps decide() as OpenClaw pre-execution hook
  - PolicyEngine constructor takes policies list — injectable, testable, no need to mock singleton
---

## Iteration 23 - US-P3-006: Test memory persistence across interactions
- Added TestAgentMemoryPersistence (5 tests) to test_openclaw_agent_basic.py
- Tests: history saved after respond, replayed in second call, no history without user_key, per-user isolation, 3-turn accumulation
- Inject MemoryAdapter(memory_root=str(tmp_path)) and mock LLM with side_effect list for multi-turn tests
- 18 total tests in file; all pass
- Files changed: tests/test_openclaw_agent_basic.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - mock_llm.generate.side_effect = list_of_replies enables multi-turn test sequences
  - call_args_list[N] for checking Nth LLM call in sequence
---

## Iteration 22 - US-P3-005: Wire memory adapter into OpenClaw agent
- Added `memory_adapter` param to `RexAgent.__init__`; defaults to `MemoryAdapter()`
- Added `user_key` kwarg to `respond()`; when set, loads history before LLM call and saves both turns after
- History skipped when `user_key=None` — all existing tests pass unchanged (54 passed)
- Files changed: rex/openclaw/agent.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Inject MemoryAdapter with tmp_path in US-P3-006 tests: `MemoryAdapter(memory_root=str(tmp_path))`
  - append_entry called twice per respond() with user_key — user turn then assistant turn
---

## Iteration 21 - US-P3-004: Audit memory_utils.py public API
- Read rex/memory_utils.py (369 lines): 9 public functions in __all__
- Three conversation-history fns (trim_history, append_history_entry, load_recent_history) already wrapped by MemoryAdapter
- Five identity/profile fns (load_users_map, resolve_user_key, load_memory_profile, load_all_profiles, extract_voice_reference) are Rex-specific — no adapter needed
- export_transcript is Rex-specific path convention — no adapter needed
- Callers: rex/memory.py re-exports all; voice_loop.py and rex_speak_api.py import directly; root memory_utils.py is compat shim for flask_proxy.py, gui.py, old tests
- Updated docs/openclaw-migration-status.md: row changed to "Audited (US-P3-004)", full audit notes appended
- 38 tests pass (no regressions)
- Learnings for future iterations:
  - Path-traversal guards (_sanitize_user_key, _validate_path_within) are security-critical; preserve in any refactor
  - Root-level memory_utils.py compat shim must stay until flask_proxy.py, gui.py migrate
---

## Iteration 20 - US-P3-003: Test memory adapter
- Created `tests/test_openclaw_memory.py` with 18 tests across 3 classes
- Tests cover: import/instantiation (4), trim_history (6), append+load (8)
- Used tmp_path fixture for all filesystem I/O; no global Memory/ directory touched
- 18 new tests pass; no regressions in existing suite
- Files changed: tests/test_openclaw_memory.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - tmp_path fixture works cleanly with memory_root override on MemoryAdapter
  - append_entry requires max_turns > 0 (ConfigurationError otherwise); tests pass explicit max_turns=50
---

## Iteration 19 - US-P3-002: Create memory adapter
- Created `rex/openclaw/memory_adapter.py` with `MemoryAdapter` class
- Wraps trim_history, append_entry, load_recent delegating to rex.memory with OpenClaw TODO stubs
- OPENCLAW_AVAILABLE guard pattern consistent with other openclaw adapters
- memory_root override supported on adapter constructor for test isolation
- 43 openclaw tests pass; module imports cleanly
- Files changed: rex/openclaw/memory_adapter.py, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - rex.memory imports (append_history_entry, load_recent_history, trim_history) are stable; no import errors
  - When building MemoryAdapter tests (US-P3-003), use tmp_path fixture for memory_root isolation
  - entry dict requires "role" and "text" keys; timestamp auto-injected by append_history_entry
---

## Iteration 18 - US-P3-001: Audit memory.py public API
- Read rex/memory.py (734 lines): documents WorkingMemory, LongTermMemory, MemoryEntry, singletons, convenience fns
- memory.py re-exports 9 functions from rex.memory_utils for backward compat
- Grepped for callers: rex/assistant.py (trim_history), rex/app.py, rex/cli.py (LTM+WM), rex_memories_bridge.py (LTM), voice_loop.py and rex_speak_api.py use rex.memory_utils directly (not rex.memory)
- Updated docs/openclaw-migration-status.md: changed status to "Audited (US-P3-001)", added full Audit Notes section
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md
- Tests pass (52 passed)
- Learnings for future iterations:
  - trim_history is highest-priority for adapter (used by assistant.py → voice loop critical path)
  - WorkingMemory and LongTermMemory use data/memory/ file storage; adapter should wrap these and add OpenClaw hook
  - MemoryEntry is a Pydantic model — tests import it but no production code imports it directly
  - rex.memory re-exports memory_utils functions; callers of voice_loop.py use memory_utils directly, not rex.memory
---

## Iteration 17 - US-P2-010: Document OpenClaw agent setup
- Created `docs/openclaw-agent-setup.md`
- Covers: package layout, bootstrap steps, config mapping table, system prompt fields, session bridge, tool registration, OPENCLAW_AVAILABLE flag, open dependencies
- 43 existing openclaw tests still pass (no regressions)
- Files changed: docs/openclaw-agent-setup.md, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Phase 2 is now complete (US-P2-001 through US-P2-010 all [x])
  - Phase 3 starts with US-P3-001: Audit memory.py public API
---

## Iteration 16 - US-P2-008 + US-P2-009: Agent smoke tests and persona verification
- Created `tests/test_openclaw_agent_basic.py` with 13 tests covering both user stories
- US-P2-008: responds non-empty to "What time is it?", LLM called once, user prompt in messages, system msg present, ValueError on empty/whitespace prompt, agent initialises without args
- US-P2-009: system_prompt contains wakeword-derived name, injected in LLM call, custom prompt overrides, agent_name from wakeword, location from config in prompt
- Key gotcha: global rex_config.json has wakeword="hey rex" → capitalize() gives "Hey rex" not "Rex"
- Fixed by using explicit AppConfig(wakeword="rex") in tests that check for "Rex" specifically
- Files changed: tests/test_openclaw_agent_basic.py, PRD-openclaw-pivot-for-rex.md
- All 13 tests pass
- Learnings for future iterations:
  - Always use explicit AppConfig in tests that check specific wakeword/agent_name values
  - Global config in this repo has wakeword="hey rex" (not "rex"), capitalize() → "Hey rex"
  - Both US-P2-008 and US-P2-009 fit in one file (test_openclaw_agent_basic.py) — no need to split
---

## Iteration 15 - US-P2-007: Register weather_now tool with OpenClaw
- Created `rex/openclaw/tools/weather_tool.py` following identical pattern to time_tool.py
- TOOL_NAME = "weather_now" (matches tool_router.py's supported_tools set — not "weather")
- weather_now(location, context) delegates to execute_tool with skip_policy/credential/audit_log=True
- register(agent=None) is the same OpenClaw stub with find_spec guard
- TOOL_DESCRIPTION notes the OPENWEATHERMAP_API_KEY requirement
- Created `tests/test_openclaw_tools_weather.py` with 11 tests
- Includes a real (non-mocked) test that verifies error dict returned when API key absent
- Files changed: rex/openclaw/tools/weather_tool.py, tests/test_openclaw_tools_weather.py, PRD-openclaw-pivot-for-rex.md
- All 11 tests pass
- Learnings for future iterations:
  - Tool name is weather_now (not weather) — always check tool_router.py supported_tools set for canonical names
  - The no-API-key test can be real (not mocked) because tool_router returns error dict without raising
---

## Iteration 14 - US-P2-006: Register time_now tool with OpenClaw
- Created `rex/openclaw/tools/__init__.py` with docstring listing planned submodules
- Created `rex/openclaw/tools/time_tool.py` with `time_now(location, context) -> dict` and `register(agent=None) -> Any`
- `time_now` delegates to `execute_tool` from tool_router (skips policy/credential/audit for direct dispatch)
- `register()` follows same find_spec + stub + warning pattern as agent.py
- TOOL_NAME = "time_now", TOOL_DESCRIPTION documents the args shape
- execute_tool imported at module level (not lazy) so it's patchable in tests
- Created `tests/test_openclaw_tools_time.py` with 12 tests
- Files changed: rex/openclaw/tools/__init__.py, rex/openclaw/tools/time_tool.py, tests/test_openclaw_tools_time.py, PRD-openclaw-pivot-for-rex.md
- All 12 tests pass
- Learnings for future iterations:
  - Import dependencies at module level (not lazily inside functions) to allow patching in tests
  - Patch target must be `rex.openclaw.tools.time_tool.execute_tool`, not `rex.tool_router.execute_tool`
  - Use skip_policy_check=True, skip_credential_check=True, skip_audit_log=True for direct tool dispatch (no side effects needed for read-only tools like time_now)
  - TOOL_NAME and TOOL_DESCRIPTION as module-level constants follow the tool registration convention
---

## Iteration 13 - US-P2-005: Wire Rex persona into OpenClaw agent
- Added `build_system_prompt(config=None) -> str` to `rex/openclaw/config.py`
- Derives persona string from AppConfig: wakeword (capitalised as agent name), active_profile (skipped when "default"), default_location, default_timezone, capabilities
- Updated `RexAgent.__init__` to accept optional `config: AppConfig | None` param
- When system_prompt is None, calls build_system_prompt(config); when agent_name is None, uses build_agent_config(config)["agent_name"]
- Replaced hardcoded "You are Rex, a helpful and friendly AI assistant." fallback
- Created `tests/test_openclaw_agent_persona.py` with 16 tests (10 for build_system_prompt, 6 for RexAgent)
- Files changed: rex/openclaw/agent.py, rex/openclaw/config.py, tests/test_openclaw_agent_persona.py, PRD-openclaw-pivot-for-rex.md
- All 23 tests pass (16 new + 7 session tests)
- Learnings for future iterations:
  - AppConfig has no persona/system_prompt field; derive from wakeword, active_profile, default_location, default_timezone, capabilities
  - agent_name = config.wakeword.capitalize() (already done in build_agent_config)
  - Import build_system_prompt inside __init__ (lazy) to avoid any potential circular import issues
  - active_profile "default" should be omitted from persona string to reduce noise
---

## Iteration 12 - US-P2-004: Create OpenClaw session bridge
- Created `rex/openclaw/session.py` with `build_session_context(explicit_user, config, metadata) -> dict`
- Bridges Rex identity resolution (resolve_active_user, get_user_profile, list_known_users) to a flat dict for OpenClaw
- Guaranteed keys: user_id, session_started_at (ISO 8601 UTC), rex_known_users, rex_user_profile
- Rex-specific extras prefixed `rex_*`; metadata dict merged directly into session
- get_user_profile exceptions are caught → rex_user_profile=None (resilient to missing profiles)
- Created `tests/test_openclaw_session.py` with 7 tests covering all branches
- session.py file already existed (untracked) from a prior partial iteration — validated and committed
- Files changed: rex/openclaw/session.py, tests/test_openclaw_session.py, PRD-openclaw-pivot-for-rex.md
- Tests pass (109 in targeted run)
- Learnings for future iterations:
  - Full `pytest tests/` is too slow (loads ML models); use targeted test runs for openclaw tasks
  - rex/identity.py exports: get_user_profile, list_known_users, resolve_active_user, set_session_user, get_session_user, clear_session_user, require_active_user, create_user_profile, update_user_preferences
  - session bridge pattern: call resolve_active_user → load profile → build dict with rex_* prefixed extras
---

## Iteration 1 - US-P1-001: Create migration tracking document
- Created `docs/openclaw-migration-status.md` with Module/Classification/Status/Notes table
- Populated with all 50+ Rex modules from Appendix A of the PRD
- Marked task complete in PRD-openclaw-pivot-for-rex.md
- Files changed: docs/openclaw-migration-status.md, PRD-openclaw-pivot-for-rex.md
- Learnings for future iterations:
  - Tests pass quickly (exit 0); torch teardown logging errors at end are harmless, not test failures
  - PRD task checkboxes are in Section 10 "Task Breakdown for Ralph Loop Execution", not Section 9 phase validation
  - Appendix A (line 1184) is the canonical module classification source
  - The progress file did not exist yet - first iteration creates it
---

## Iteration 2 - US-P1-002: Add OPENCLAW-REPLACE markers
- Prepended `# OPENCLAW-REPLACE: This module will be replaced by OpenClaw. Do not add new features.` to 12 files
- Used a shell loop with mktemp to prepend comment + blank line before existing docstring
- Files changed: rex/event_bus.py, rex/tool_registry.py, rex/tool_router.py, rex/plugin_loader.py, rex/browser_automation.py, rex/dashboard/{__init__.py,routes.py,sse.py,auth.py}, rex/dashboard_store.py, rex/messaging_service.py, rex/executor.py, PRD-openclaw-pivot-for-rex.md
- Tests pass (exit 0); torch teardown logging errors at end are harmless
- Learnings for future iterations:
  - All 12 target files existed; none were missing
  - All files started with docstrings ("""); marker goes BEFORE the docstring as a comment line
  - Shell mktemp prepend pattern works cleanly: echo marker > tmp; echo "" >> tmp; cat file >> tmp; mv tmp file
  - Check for existing marker with grep -q before prepending to make script idempotent
---

## Iteration 3 - US-P1-003: Add OPENCLAW-WRAP markers
- Prepended `# OPENCLAW-WRAP: This module will be wrapped around OpenClaw. Preserve public API.` to 10 files
- Files changed: rex/autonomy/{__init__,runner,llm_planner,rule_planner}.py, rex/policy.py, rex/policy_engine.py, rex/identity.py, rex/profile_manager.py, rex/workflow.py, rex/workflow_runner.py, PRD-openclaw-pivot-for-rex.md
- Tests pass (exit 0)
- Learnings: same mktemp prepend pattern; all files existed
---

## Iteration 4 - US-P1-004: Extract tool routing Protocol
- Created `rex/contracts/tool_routing.py` with `ToolRoutingProtocol` (runtime_checkable Protocol)
- Public API: parse_tool_request, execute_tool, route_if_tool_request
- format_tool_result excluded (presentation concern, not routing)
- rex/contracts/ already existed with __init__.py, core.py, version.py
- Tests pass (exit 0)
- Learnings:
  - tool_router.py public functions: parse_tool_request, execute_tool, format_tool_result, route_if_tool_request
  - Protocol methods use instance-method style (self as first arg)
  - Use `from __future__ import annotations` for forward refs in Protocol files
---

## Iteration 5 - US-P1-005: Extract event bus Protocol
- Created `rex/contracts/event_bus.py` with `EventBusProtocol` (runtime_checkable Protocol)
- Public API: subscribe, unsubscribe, publish, get_metrics, get_stats, iter_subscribers, get_subscription_count, clear_subscriptions
- Used `AnyHandler = Callable[..., None]` to stay dependency-free (avoids importing Event from event_bus)
- event_bus.py has dual API: legacy (str, dict) and rich (Event object) - both documented in Protocol
- Tests pass (exit 0)
---

## Iteration 11 - US-P2-003: Create OpenClaw config bridge
- Created `rex/openclaw/config.py` with `build_agent_config(config=None) -> dict`
- Maps AppConfig fields: identity (agent_name from wakeword, user_id, active_profile), LLM params, context (location, timezone), memory limits
- Rex-specific extras prefixed `rex_*` for future remapping once OpenClaw config schema is known
- config=None → loads global config via load_config()
- agent_name uses `config.wakeword.capitalize()` → "Rex" (wakeword is "rex" by default)
- Tests pass (exit 0)
- Learnings:
  - AppConfig uses `AppConfig` not `Settings` (PRD task name was slightly wrong)
  - wakeword field is the canonical agent name source (it's "rex" by default)
  - rex_* prefix convention keeps Rex-specific values identifiable until OpenClaw schema is known
---

## Iteration 10 - US-P2-002: Create OpenClaw agent registration module
- Created `rex/openclaw/agent.py` with `RexAgent` class
- `respond(prompt)` calls `LanguageModel.generate(messages=[system, user])` with Rex persona system prompt
- `register()` is a documented stub guarded by `find_spec("openclaw")` — no-op + warning when openclaw not installed
- `OPENCLAW_AVAILABLE` module-level flag follows same find_spec pattern as browser_automation.py (playwright) and llm_client.py
- Lazy LLM property instantiation avoids loading model weights at import time
- openclaw package is not installed; open dependency per PRD §8.3
- Tests pass (exit 0)
- Learnings:
  - When OpenClaw API is unknown, implement the class with a documented TODO stub for the registration call
  - find_spec guard makes the module importable without openclaw installed
  - System prompt must be injected at the `messages` level (not as a prepend to the user string) for proper role separation
---

## Iteration 9 - US-P2-001: Create rex/openclaw subpackage
- Created `rex/openclaw/__init__.py` with module docstring
- Docstring describes the subpackage's role and lists expected submodules (agent, config, session, tools) as future additions
- No submodules auto-imported (avoids side effects at package import time)
- Tests pass (exit 0)
- Learnings:
  - rex/openclaw/ did not exist; had to create both directory and __init__.py
  - Simple __init__.py with docstring is all that's needed for a skeleton package
---

## Iteration 8 - US-P1-008: Extract plugin/tool loading Protocol
- Created `rex/contracts/plugins.py` with two @runtime_checkable Protocols
- `PluginLoaderProtocol`: single method — load_plugins(path) -> dict[str, Any]
- `ToolRegistryProtocol`: full ToolRegistry API — register_tool, unregister_tool, get_tool, has_tool, list_tools, check_credentials, validate_credentials_for_tool, check_health, check_all_health, get_tool_status, get_all_status
- plugin_loader.py is function-based (not class-based); PluginLoaderProtocol wraps it as a service method for structural substitutability
- Used Any for ToolMeta params to stay dependency-free (ToolMeta is defined in tool_registry.py which will be replaced)
- Tests pass (exit 0)
- Learnings:
  - A module-level function can be contracted as a Protocol method — any class that implements the method satisfies the Protocol
  - HealthCheckFn type alias (Callable[[], tuple[bool, str]]) is an implementation detail; Protocol uses tuple[bool, str] return types directly
---

## Iteration 7 - US-P1-007: Extract dashboard Protocol
- Created `rex/contracts/dashboard.py` with three @runtime_checkable Protocols
- `SessionManagerProtocol`: auth API — create_session, validate_session, invalidate_session, get_active_session_count
- `NotificationBroadcasterProtocol`: SSE API — subscribe, unsubscribe, publish, stream, shutdown, subscriber_count (property)
- `DashboardProtocol`: top-level service — session_manager (property), broadcaster (property), register_routes(app)
- Three protocols because dashboard has three independently-replaceable concerns: auth, SSE, and route registration
- Used `Any` for session/subscriber objects to stay dependency-free (avoids importing Session/`_Subscriber` dataclasses)
- `register_routes` takes `Any` for the app arg (avoids importing Flask — OpenClaw may use a different framework)
- `subscriber_count` is a property in NotificationBroadcaster; modeled as `@property` in Protocol
- Tests pass (exit 0)
---

## Iteration 6 - US-P1-006: Extract browser automation Protocol
- Created `rex/contracts/browser.py` with two @runtime_checkable Protocols
- `BrowserSessionProtocol`: async session API — launch, close, navigate, click, type_text, wait, wait_for_selector, screenshot, login, download_file, get_content, get_text
- `BrowserAutomationProtocol`: service-level API — execute_script, list_sessions, list_screenshots
- Two protocols needed because browser_automation.py has two classes: BrowserSession (low-level async) and BrowserAutomationService (Rex integration layer)
- Module-level functions (run_browser_script, run_browser_script_sync, get/set/reset_browser_service) are implementation helpers, not part of the substitutable interface
- Tests pass (exit 0)
- Learnings:
  - When a module has both a session class and a service class, define separate Protocols for each
  - All BrowserSession methods are async; use `async def` in Protocol stubs
  - Optional[str] import needed because browser_automation.py uses Optional from typing (not str | None union style)
---
