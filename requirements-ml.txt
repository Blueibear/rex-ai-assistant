# Optional machine learning dependencies for speech and language features
# Prefer extras and split requirements:
#   pip install -e ".[ml]"
#   pip install -r requirements-cpu.txt
#   pip install -r requirements-gpu-cu124.txt
