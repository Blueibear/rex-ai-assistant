# CPU-only ML dependencies for Rex AI Assistant (no CUDA, no extra index URL)
# Use this for CPU-only deployments or CI environments that need the ML stack.

# PyTorch CPU versions (PyPI CPU wheels)
# Verified working stack uses torch 2.6.0 with TTS 0.22.0 on Python 3.11.
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# Install Rex with ML + audio extras
-e ".[ml,audio]"
