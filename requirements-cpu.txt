# CPU-only ML dependencies for Rex AI Assistant (no CUDA, no extra index URL)
# Use this for CPU-only deployments or CI environments that need the ML stack.

# PyTorch CPU versions (PyPI CPU wheels)
# torch matches the packaged Windows Voice runtime pin in
# requirements-electron-voice.txt; torchvision/torchaudio are the pip-resolved
# companion releases for that torch version.
torch==2.12.1
torchvision==0.27.1
torchaudio==2.11.0

# transformers: pin to a semver-compatible range for Coqui TTS 0.22.0.
# BeamSearchScorer was removed in 4.38+; the rex/compat/ shim restores it.
transformers>=4.38.0,<5.0

# Install Rex with ML + audio extras
-e ".[ml,audio]"
